import { Lesson } from '../models/lesson.model';

export const VUE_EXTRA_LESSONS: Lesson[] = [
  {
    id: 516, slug: 'vue-options-api', title: 'Options API',
    description: 'Master the Options API — Vue\'s classic component definition style with data, methods, computed, and lifecycle hooks.',
    level: 'beginner', duration: '35 min',
    objectives: ['Define components using the Options API syntax', 'Understand the relationship between data, computed, methods, and watchers', 'Choose between Options API and Composition API for different use cases'],
    topics: [
      { id: 'options-api-basics', title: 'Options API Basics', content: 'The Options API is Vue\'s original way of organizing component logic. When you create a component you export an object with predefined option keys. The data function returns a reactive object whose properties become component state. The template accesses these properties directly by name. Methods are functions defined on the component instance that can be called from templates or other methods. Computed properties derive values from reactive state and cache results until dependencies change. Watchers run side effects when specific reactive properties change. Each option serves a clear purpose and the Options API enforces a structured approach to organizing code. Components defined this way have their state and behavior separated into distinct option categories which makes it easy for developers to find related logic at a glance.' },
      { id: 'data-and-reactivity', title: 'Data and Reactivity', content: 'In the Options API the data option must be a function that returns a plain JavaScript object. Vue converts this object into a reactive proxy so any changes to its properties trigger re-renders. The returned object should not reference external mutable state or the component instance itself. You can define nested objects and arrays and Vue will make them deeply reactive. However there are caveats — Vue 3 cannot detect property additions on already-created objects so you must declare all reactive properties upfront in the data return. For arrays Vue intercepts mutation methods like push and splice to trigger updates. The component instance exposes all data properties directly so you can access this.count instead of needing a ref wrapper. This direct access makes templates clean but means you cannot destructure data in the setup phase without losing reactivity.' },
      { id: 'computed-and-methods', title: 'Computed vs Methods', content: 'Computed properties and methods both return values but differ in caching behavior. A computed property caches its result based on reactive dependencies. When none of the dependencies change the cached value is returned without re-executing the getter. This is ideal for expensive derivations like sorting or filtering lists. A method re-executes on every re-render whenever the component re-renders regardless of whether its inputs changed. Use methods for actions that should always run like event handlers or API calls. The template accesses computed properties without parentheses while methods require parentheses. A common mistake is using methods where computed should be used which wastes performance by recalculating on every render. Another mistake is using computed for side effects since computed properties should be pure functions with no side effects. If you need to run side effects when a value changes use a watcher instead.' }
    ],
    quiz: [
      { id: 1, question: 'Why must the data option be a function in the Options API?', options: ['Vue cannot parse plain objects', 'It ensures each component instance gets its own copy of state', 'It enables TypeScript support', 'It allows async data loading'], correctIndex: 1, explanation: 'data must be a function so each component instance receives its own reactive state object. If it were a plain object all instances would share the same reference.' },
      { id: 2, question: 'What is the main difference between computed properties and methods?', options: ['Methods are reactive and computed is not', 'Computed properties cache results based on dependencies', 'Methods can only be used in templates', 'Computed properties run on every render'], correctIndex: 1, explanation: 'Computed properties cache their results based on reactive dependencies. Methods re-execute on every render making computed more efficient for derived values.' },
      { id: 3, question: 'What happens if you add a new property to a data object after the component is created in Vue 3?', options: ['The property is automatically reactive', 'The property is not reactive because Vue 3 cannot detect new property additions on proxies', 'Vue throws a runtime error', 'The component re-renders immediately'], correctIndex: 1, explanation: 'Vue 3 uses Proxy-based reactivity so new properties added after creation are not automatically reactive. You must declare all reactive properties upfront.' }
    ]
  },
  {
    id: 517, slug: 'vue-reactivity-deep', title: 'Reactivity Deep Dive',
    description: 'Understand Vue\'s reactivity system at a deep level including Proxy, ref, reactive, and computed tracking.',
    level: 'intermediate', duration: '40 min',
    objectives: ['Explain how Vue 3 reactivity works under the hood with Proxy', 'Use ref and reactive appropriately in different scenarios', 'Avoid common reactivity pitfalls like lost reactivity and stale closures'],
    topics: [
      { id: 'proxy-reactivity', title: 'Proxy-Based Reactivity', content: 'Vue 3 replaced Object.defineProperty with ES6 Proxy for its reactivity system. A Proxy wraps an object and intercepts all operations including property access set deletion and method calls. When you call reactive() Vue creates a Proxy over your object that tracks which properties are read during a component render or computed evaluation. When a tracked property is later set the Proxy triggers all dependent effects to re-run. This means Vue 3 can detect new property additions and array index mutations without special syntax. The get trap calls track() to register the current effect as a dependency and the set trap calls trigger() to notify dependents. This approach is more powerful and complete than Vue 2 which required Vue.set for new properties. The tradeoff is that Proxy is not supported in IE11 but modern browsers all support it.' },
      { id: 'ref-vs-reactive', title: 'Ref vs Reactive', content: 'ref() wraps a single value in a reactive reference object with a .value property. It works with primitives like strings numbers and booleans which cannot be Proxied directly. When you access ref in templates it auto-unwraps so you do not need .value in the template. reactive() wraps an entire object deeply making all nested properties reactive without needing .value. Choose ref when you have a single value or when you need to reassign the entire reference. Choose reactive when you have a complex object with multiple reactive properties you access together. A common mistake is destructuring a reactive object which breaks reactivity because the destructured variables are plain values. Use toRefs() to destructure while preserving reactivity by creating individual refs for each property. Another consideration is that ref works better with TypeScript because the type is more explicit.' },
      { id: 'reactivity-pitfalls', title: 'Reactivity Pitfalls', content: 'Several common pitfalls can cause reactivity to break silently. Destructuring reactive objects loses reactivity because the extracted values are snapshots not reactive references. Destructure with toRefs to keep each property connected to the original reactive object. Replacing a reactive object with a new one does not work because the Proxy is bound to the original object. Mutate the existing object properties instead or use ref which allows reassignment via .value. When passing reactive objects as function arguments the function receives the same proxy reference. If you spread a reactive object into a new object the spread result is not reactive. Reactive objects also cannot hold primitives directly so use ref for that. The toRaw() function returns the original unproxied object which is useful for debugging or passing to third-party libraries that do not work with Proxies. The shallowRef and shallowReactive variants only track the top level providing performance optimization for large objects where deep reactivity is unnecessary.' }
    ],
    quiz: [
      { id: 1, question: 'Why does Vue 3 use Proxy instead of Object.defineProperty?', options: ['Proxy is faster for simple objects', 'Proxy can intercept property additions, deletions, and array mutations', 'Proxy uses less memory', 'Object.defineProperty is deprecated'], correctIndex: 1, explanation: 'Proxy intercepts all object operations including new property access which allows Vue 3 to detect changes that Object.defineProperty could not track.' },
      { id: 2, question: 'What happens when you destructure a reactive object without toRefs?', options: ['The destructured values are reactive', 'Reactivity is lost because the values are plain snapshots', 'Vue throws an error', 'The component crashes'], correctIndex: 1, explanation: 'Destructuring extracts plain values from the reactive proxy. These snapshots are not connected to the reactive system so changes to them do not trigger re-renders.' },
      { id: 3, question: 'When should you use ref instead of reactive?', options: ['Always use reactive for objects', 'When you need to hold primitive values or reassign the entire reference', 'When you want deeper reactivity', 'When working with TypeScript only'], correctIndex: 1, explanation: 'ref works with primitives and allows full reassignment via ref.value. reactive cannot hold primitives and replacing the entire object breaks reactivity.' }
    ]
  },
  {
    id: 518, slug: 'vue-computed-watch', title: 'Computed Properties and Watchers',
    description: 'Leverage computed properties for derived state and watchers for side effects in Vue components.',
    level: 'beginner', duration: '30 min',
    objectives: ['Create computed properties for efficient derived state', 'Use watch and watchEffect for side effects', 'Understand when to use computed versus watchers versus methods'],
    topics: [
      { id: 'computed-properties', title: 'Computed Properties', content: 'Computed properties derive values from reactive state and automatically update when dependencies change. They are defined as functions that return a value based on reactive data. The result is cached so the function only re-runs when a dependency actually changes. This makes them efficient for expensive operations like filtering sorting or transforming data. Computed properties are lazy meaning they do not execute until first accessed. They must be pure functions with no side effects meaning they should not modify other state or trigger async operations. In the Options API you define them in the computed option as getter functions or getter-setter pairs. In the Composition API you use the computed function from vue which returns a readonly ref. Computed properties are ideal for template expressions that depend on multiple reactive values or require computation that would clutter the template.' },
      { id: 'watchers', title: 'Watchers', content: 'Watchers run side effects in response to changes in reactive state. Unlike computed properties which are synchronous and pure watchers can perform async operations like API calls or DOM manipulation. The watch function takes a reactive source and a callback that receives the new value and previous value. You can watch a single ref a getter function or an array of sources. The flush option controls when the callback runs relative to component updates with pre running before DOM updates post running after and sync running synchronously on every change. Watchers are lazy by default meaning they only run when the watched source actually changes. The watchEffect function runs the callback immediately and automatically tracks all reactive dependencies used inside it. Use watch when you need both old and new values. Use watchEffect for side effects that depend on multiple reactive sources without needing to list them explicitly.' },
      { id: 'computed-vs-watch', title: 'Computed vs Watch vs Methods', content: 'Choosing the right tool depends on whether you need a derived value or a side effect. Computed properties return a derived value and should be pure with no side effects. They cache results and only recalculate when dependencies change making them efficient for template bindings. Methods execute on every re-render and are best for event handlers or actions that should always run. Watchers respond to reactive changes and execute side effects like logging analytics fetching data on input changes or syncing state across stores. A common anti-pattern is using watchers to maintain derived state when computed would be more efficient. If a value depends on other reactive values it should be computed not watched. Another anti-pattern is using computed for async operations since computed must return synchronously. If you need to fetch data when a value changes use watch instead. For simple one-off derivations in templates an inline expression or method call may suffice without the overhead of a computed property.' }
    ],
    quiz: [
      { id: 1, question: 'What makes computed properties efficient compared to methods?', options: ['They execute only once', 'They cache results and only recalculate when dependencies change', 'They bypass the reactivity system', 'They run asynchronously'], correctIndex: 1, explanation: 'Computed properties cache their results. They only re-execute when their reactive dependencies change avoiding redundant computation on every render.' },
      { id: 2, question: 'When should you use watchEffect over watch?', options: ['When you need the previous value', 'When you have side effects that depend on multiple reactive sources without needing explicit listing', 'When you want synchronous execution only', 'When you need to prevent default behavior'], correctIndex: 1, explanation: 'watchEffect automatically tracks all reactive dependencies used inside the callback making it simpler than watch when you have multiple dependencies.' },
      { id: 3, question: 'Why should you not use computed for async data fetching?', options: ['Computed is too slow', 'Computed must return a value synchronously and cannot return promises', 'Vue does not allow async in computed', 'Computed does not support dependencies'], correctIndex: 1, explanation: 'Computed properties must return synchronously. They cannot return promises or use async/await. Use watch or watchEffect for async operations.' }
    ]
  },
  {
    id: 519, slug: 'vue-lifecycle-deep', title: 'Lifecycle Hooks Deep Dive',
    description: 'Master Vue lifecycle hooks in both Options API and Composition API with practical timing insights.',
    level: 'intermediate', duration: '35 min',
    objectives: ['Map Options API lifecycle hooks to Composition API equivalents', 'Understand hook execution timing relative to DOM updates', 'Use lifecycle hooks for proper resource management and cleanup'],
    topics: [
      { id: 'lifecycle-overview', title: 'Lifecycle Overview', content: 'Every Vue component goes through a series of initialization steps when it is created. The lifecycle represents the journey from creation to destruction. In the Options API the key hooks are beforeCreate created beforeMount mounted beforeUpdate updated beforeUnmount and unmounted. In the Composition API the equivalents are setup (replacing beforeCreate and created) onBeforeMount onMounted onBeforeUpdate onUpdated onBeforeUnmount and onUnmounted. setup runs before the component is created so it has no access to the template or DOM. onMounted runs after the component is inserted into the DOM making it ideal for DOM manipulation API calls and third-party library initialization. onUnmounted runs after the component is removed from the DOM and is the place to clean up timers event listeners and subscriptions. Understanding the exact timing of each hook prevents bugs caused by accessing the DOM too early or cleaning up resources too late.' },
      { id: 'composition-lifecycle', title: 'Composition API Lifecycle', content: 'In the Composition API lifecycle hooks are imported from vue and called inside setup or script setup. onMounted registers a callback that runs after the component is mounted to the DOM. You can register multiple onMounted callbacks and they execute in registration order. onUpdated fires after a reactive state change causes the DOM to be re-rendered. Avoid mutating state inside onUpdated as it can cause infinite loops. onBeforeUnmount runs just before the component is removed giving you a chance to clean up. onUnmounted runs after removal when the component is fully destroyed. The setup function itself replaces both beforeCreate and created hooks. Code at the top level of setup runs during the created phase. For async operations use onMounted to ensure the DOM is available. The activated and deactivated hooks work with keep-alive to handle component caching transitions. ErrorCaptured handles errors thrown in descendant components.' },
      { id: 'resource-management', title: 'Resource Management', content: 'Proper resource management prevents memory leaks and keeps applications performant. The most common pattern is setting up resources in onMounted and cleaning them up in onUnmounted. Start intervals and timers in onMounted and clear them in onUnmounted with clearInterval and clearTimeout. Add event listeners in onMounted and remove them in onUnmounted to prevent listener leaks. Subscribe to external data sources like WebSocket connections or observables in onMounted and unsubscribe in onUnmounted. When using third-party libraries that create DOM elements initialize them in onMounted and call their destroy methods in onUnmounted. The onBeforeUnmount hook is useful when you need to perform cleanup before the DOM element is removed such as saving scroll position or triggering exit animations. Always pair resource creation with corresponding cleanup to maintain application health. The return value of setup is not used for cleanup in Vue 3 unlike React hooks.' }
    ],
    quiz: [
      { id: 1, question: 'Which Composition API hook replaces both beforeCreate and created?', options: ['onMounted', 'setup', 'onBeforeMount', 'created'], correctIndex: 1, explanation: 'The setup function runs before the component is created making it equivalent to both beforeCreate and created hooks from the Options API.' },
      { id: 2, question: 'Where should you initialize a third-party library that needs DOM access?', options: ['setup', 'onBeforeMount', 'onMounted', 'onCreated'], correctIndex: 2, explanation: 'onMounted runs after the component is inserted into the DOM so the DOM element is available for third-party library initialization.' },
      { id: 3, question: 'What is the primary purpose of onUnmounted?', options: ['To save component state', 'To clean up resources like timers event listeners and subscriptions', 'To re-render the component', 'To update reactive state'], correctIndex: 1, explanation: 'onUnmounted runs after the component is removed from the DOM and is the place to clean up timers event listeners subscriptions and other resources to prevent memory leaks.' }
    ]
  },
  {
    id: 520, slug: 'vue-template-refs', title: 'Template Refs',
    description: 'Access DOM elements and child component instances directly using template refs in Vue.',
    level: 'intermediate', duration: '30 min',
    objectives: ['Create and access DOM elements with template refs', 'Use refs to interact with child component instances', 'Understand the timing of ref availability relative to lifecycle hooks'],
    topics: [
      { id: 'template-ref-basics', title: 'Template Ref Basics', content: 'Template refs provide direct access to DOM elements or child component instances. In the Composition API you create a ref variable with ref(null) and bind it to an element using the ref attribute in the template. After the component mounts the ref variable holds the actual DOM element. This is useful for focusing inputs measuring element dimensions scrolling to specific elements or integrating with libraries that require a DOM node. In the Options API you declare refs in the refs option as strings and access them via this.$refs. The ref attribute value must match the variable name. You can use dynamic refs by passing a function to the ref attribute which receives the element as an argument. Dynamic refs are useful when rendering lists where each item needs its own ref. The ref value is null during setup and before mounting because the DOM does not exist yet. Access refs inside onMounted or in event handlers where the DOM is guaranteed to be present.' },
      { id: 'refs-on-components', title: 'Refs on Child Components', content: 'When you put a ref on a child component the ref holds the component instance not the DOM element. This gives you access to the child\'s exposed properties and methods. In the Composition API the child component must use defineExpose to explicitly expose properties and methods. Without defineExpose the parent cannot access anything on the child instance. This is a deliberate design decision for better encapsulation. In the Options API all properties and methods are accessible by default since the child exposes everything. The parent can call child methods access child data and read child computed properties. However directly accessing child internals creates tight coupling. Prefer using props and events for parent-child communication. Use component refs only when you genuinely need imperative access such as calling a method on a form component to trigger validation or accessing a scroll container\'s position. Always handle the case where the ref is null since it is null before mount.' },
      { id: 'ref-timing', title: 'Ref Timing', content: 'Understanding when refs become available is critical to avoid null reference errors. During setup template refs are always null because the template has not been rendered yet. During onBeforeMount the template is compiled but not yet inserted into the DOM so refs are still null. During onMounted the DOM is fully rendered and all refs are populated with their respective elements or component instances. This is the earliest safe point to access DOM refs. During onUpdated refs point to the latest DOM elements after a re-render. After onUnmounted refs become null again as the DOM elements are removed. In event handlers like @click refs are always available since the DOM exists. For watchers that track template changes refs reflect the current DOM state. If you need to access a ref immediately after a reactive state change that affects the DOM use nextTick to wait for the DOM update before reading the ref. This ensures you get the updated element rather than the stale one.' }
    ],
    quiz: [
      { id: 1, question: 'What does a ref on a child component hold in Vue 3?', options: ['The DOM element', 'The component instance with only explicitly exposed properties', 'The template content', 'A reactive data object'], correctIndex: 1, explanation: 'In Vue 3 a ref on a child component holds the component instance but only properties and methods explicitly exposed via defineExpose are accessible.' },
      { id: 2, question: 'When is the earliest safe time to access a template ref?', options: ['During setup', 'During onBeforeMount', 'During onMounted', 'During onUpdated'], correctIndex: 2, explanation: 'onMounted runs after the DOM is fully rendered making it the earliest safe point to access template refs without encountering null values.' },
      { id: 3, question: 'What should you do if you need to read a ref after a reactive state change that affects the DOM?', options: ['Use onMounted again', 'Wrap the ref access in nextTick', 'Use a setTimeout', 'Access the ref immediately'], correctIndex: 1, explanation: 'nextTick waits for the DOM to update after a reactive state change ensuring you read the current DOM element rather than the stale version.' }
    ]
  },
  {
    id: 521, slug: 'vue-provide-inject', title: 'Provide/Inject',
    description: 'Share data across component trees without prop drilling using provide and inject.',
    level: 'intermediate', duration: '30 min',
    objectives: ['Provide values from ancestor components to descendants', 'Use inject with default values for flexible component APIs', 'Make provided values reactive with ref and reactive'],
    topics: [
      { id: 'provide-inject-basics', title: 'Provide/Inject Basics', content: 'Provide and inject allow an ancestor component to provide data to any descendant component regardless of how deep the component tree goes. This avoids prop drilling where you pass props through multiple intermediate components that do not need them. In the Composition API the parent calls provide(key value) inside setup to make a value available. Descendant components call inject(key defaultValue) to receive it. The key can be a string or a Symbol for avoiding naming conflicts. The value can be any JavaScript type including reactive objects. Provide and inject are not reactive by default meaning if the parent changes the provided value the children do not automatically update. To make them reactive provide a ref or reactive object and the children will see updates. This pattern is useful for theme configuration authentication state and dependency injection in library components.' },
      { id: 'reactive-provide', title: 'Reactive Provide', content: 'To make provided values reactive across the component tree you need to provide ref or reactive objects. When you provide a plain value like a string or number the children receive a snapshot that does not update when the parent changes it. Instead provide a ref and the children access ref.value which stays connected to the source. You can also provide a reactive object and all properties remain reactive in descendants. A powerful pattern is using computed inside the provider to create derived reactive values that children automatically receive. The use of provide with computed creates a reactive dependency chain where children always see the latest computed value. For providing methods like event handlers simply provide the function reference and children can call it to communicate back up the tree. This creates a clean dependency injection pattern where the parent controls state and children interact through provided methods.' },
      { id: 'provide-inject-patterns', title: 'Provide/Inject Patterns', content: 'Provide and inject work well for several architectural patterns. Theme systems use provide to distribute color schemes and typography settings to all descendant components. Authentication contexts provide user information and login methods to the entire component tree. Form libraries use provide to share form state and validation methods with nested form fields. Library authors use provide with Symbols as keys to avoid conflicts with consumer code. A common pattern is providing a reactive state object along with mutation methods so children can both read and modify shared state. TypeScript users can create typed provide and inject helpers using InjectionKey symbols to ensure type safety. Provide and inject should not be used for deeply shared state that changes frequently as it can be hard to trace data flow. For application-wide state prefer Pinia. Use provide and inject for component library internals or localized tree-wide data like themes and form contexts.' }
    ],
    quiz: [
      { id: 1, question: 'What problem does provide/inject solve?', options: ['Component styling', 'Prop drilling through intermediate components', 'Reactivity performance', 'Template compilation'], correctIndex: 1, explanation: 'provide/inject allows ancestor components to pass data directly to deep descendants without intermediate components needing to pass props through.' },
      { id: 2, question: 'Why should you provide a ref instead of a plain value for reactivity?', options: ['Refs are faster', 'Plain values are snapshots and do not update in children when the parent changes', 'Refs use less memory', 'Plain values cannot be used in templates'], correctIndex: 1, explanation: 'Providing a plain value gives children a snapshot. A ref maintains the reactive connection so children see updated values when the parent changes them.' },
      { id: 3, question: 'What is a recommended use case for provide/inject?', options: ['Application-wide state management', 'Component library internals like theming and form contexts', 'URL routing', 'Server-side rendering'], correctIndex: 1, explanation: 'provide/inject is ideal for component library internals like theming and form contexts where data needs to flow through a tree without prop drilling.' }
    ]
  },
  {
    id: 522, slug: 'vue-custom-directives', title: 'Custom Directives',
    description: 'Extend Vue\'s template capabilities with custom directives for reusable DOM behaviors.',
    level: 'intermediate', duration: '35 min',
    objectives: ['Create custom directives with lifecycle hooks', 'Register directives globally or locally', 'Use directive arguments and modifiers for flexible behavior'],
    topics: [
      { id: 'directive-basics', title: 'Directive Basics', content: 'Custom directives let you attach reusable behavior to DOM elements directly in your templates. A directive is defined as an object with lifecycle hooks that receive the element and binding information. The main hooks are mounted (called when the element is inserted into the DOM) updated (called after the containing component updates) and unmounted (called when the element is removed). Each hook receives the raw DOM element as the first argument and a binding object as the second. The binding object contains the value (the current value passed to the directive) oldValue (previous value) arg (argument after the colon) modifiers (object of modifiers) and the element itself. You use directives in templates with the v-prefix like v-focus or v-tooltip. Global directives are registered with app.directive while local directives are declared in the directives option of a component. Global directives are available everywhere while local ones are scoped to the declaring component.' },
      { id: 'directive-hooks', title: 'Directive Lifecycle Hooks', content: 'Custom directive hooks map to component lifecycle events. The created hook runs when the element\'s attributes are set but before it is inserted into the DOM. The beforeMount hook runs when the element is about to be inserted. Mounted runs after the element is in the DOM and is the most commonly used hook for setup logic. beforeUpdate and updated run when the binding value changes and the parent component re-renders. beforeUnmount and unmounted run when the element is removed from the DOM. The updated hook is critical for keeping the DOM in sync with the directive value. Without it the directive only applies its effect once and does not respond to value changes. For performance consider checking if the value actually changed in the updated hook before reapplying the effect. The unmounted hook should clean up any event listeners timers or observers created in mounted to prevent memory leaks.' },
      { id: 'directive-args-modifiers', title: 'Arguments and Modifiers', content: 'Directives can accept arguments and modifiers for flexible configuration. An argument is specified after a colon in the directive like v-tooltip:top where top is the argument accessible via binding.arg. This lets a single directive support different modes or targets. Modifiers are dot-separated flags like v-click-outside.escape where escape is a modifier accessible via binding.modifiers as an object with boolean properties. Arguments can be dynamic using square brackets like v-tooltip:[position]. The binding object also provides binding.instance (component instance) and binding.dir (the directive definition). A powerful pattern is combining arguments and modifiers for highly configurable directives. For example a v-resize directive could accept a modifier for debounce behavior and an argument for the resize direction. Directive arguments and modifiers make the template syntax declarative and self-documenting. They let template readers understand behavior at a glance without reading JavaScript code.' }
    ],
    quiz: [
      { id: 1, question: 'Which directive hook runs after the element is inserted into the DOM?', options: ['created', 'beforeMount', 'mounted', 'updated'], correctIndex: 2, explanation: 'The mounted hook runs after the element is inserted into the DOM making it the right place for DOM manipulation and setup.' },
      { id: 2, question: 'How do you access a directive argument in the binding object?', options: ['binding.value', 'binding.arg', 'binding.modifiers', 'binding.instance'], correctIndex: 1, explanation: 'binding.arg contains the argument specified after the colon in the directive such as the top in v-tooltip:top.' },
      { id: 3, question: 'What is the difference between global and local directive registration?', options: ['Global directives are faster', 'Global directives are available everywhere while local directives are scoped to the declaring component', 'Local directives support modifiers but global ones do not', 'There is no difference'], correctIndex: 1, explanation: 'Global directives registered with app.directive are available in all templates. Local directives declared in the directives option are only available in that component.' }
    ]
  },
  {
    id: 523, slug: 'vue-plugins', title: 'Vue Plugins',
    description: 'Extend Vue\'s global functionality by creating and using plugins.',
    level: 'intermediate', duration: '30 min',
    objectives: ['Create plugins using the install method pattern', 'Add global properties directives and components via plugins', 'Structure plugins for reusability and configuration'],
    topics: [
      { id: 'plugin-basics', title: 'Plugin Basics', content: 'A Vue plugin is an object or function that adds global-level functionality to Vue. The simplest plugin is an object with an install method that receives the app instance and optional options. Plugins can add global properties (accessible on every component instance) global directives (usable in any template) global components (renderable anywhere) and inject or provide values to the entire application. You install a plugin by calling app.use(plugin options) before mounting the app. app.use calls the plugin\'s install method once even if called multiple times with the same plugin. Plugins are the primary way to extend Vue Router Pinia and other official libraries integrate with Vue applications. When building a component library plugins provide a convenient installation API for consumers. Plugins can also add global methods by attaching them to the app.config.globalProperties object.' },
      { id: 'plugin-patterns', title: 'Plugin Patterns', content: 'Common plugin patterns include global component registration global directive installation and global method injection. A plugin that adds a global notification method might install a function on app.config.globalProperties that component instances can call via this.$notify. A plugin providing a modal system might register a global Modal component and a v-modal directive. For logging plugins install a $log method that wraps console methods with additional context. Plugin options allow consumers to configure behavior. The install method receives these options and uses them to customize what gets added to the application. A well-designed plugin exports its installer function separately so tree-shaking can remove unused parts. Plugins can also use app.provide to inject services that all components can access via inject. This combines the plugin pattern with dependency injection for a clean architecture.' },
      { id: 'plugin-lifecycle', title: 'Plugin Lifecycle and Design', content: 'Plugins run during application initialization when app.use is called. The install function executes synchronously before the app mounts. This means all global registrations and configurations are available immediately. Plugins should be designed to be idempotent meaning calling app.use with the same plugin multiple times should not cause issues. Store a flag on the app instance to prevent double installation. For plugins that depend on other plugins check for their presence before relying on their features. A plugin can also export helper functions alongside the install method for consumers who prefer explicit imports over global registration. Test plugins by creating a minimal app instance and verifying the plugin adds the expected functionality. Document the plugin options clearly and provide sensible defaults. Consider backward compatibility when updating plugins as breaking changes affect all consumers.' }
    ],
    quiz: [
      { id: 1, question: 'What does the install method of a Vue plugin receive?', options: ['The component instance and props', 'The app instance and options', 'The router and store', 'The template and compiler'], correctIndex: 1, explanation: 'The install method receives the Vue app instance and optional plugin options allowing the plugin to add global functionality.' },
      { id: 2, question: 'When does a plugin\'s install function execute?', options: ['On every component mount', 'During application initialization when app.use is called', 'When the router navigates', 'On first user interaction'], correctIndex: 1, explanation: 'The install function runs synchronously during application initialization before the app is mounted.' },
      { id: 3, question: 'What should a well-designed plugin do if installed multiple times?', options: ['Throw an error', 'Ignore subsequent installations (be idempotent)', 'Replace the previous installation', 'Uninstall itself'], correctIndex: 1, explanation: 'Plugins should be idempotent meaning calling app.use with the same plugin multiple times should not cause duplicate functionality or errors.' }
    ]
  },
  {
    id: 524, slug: 'vue-mixins', title: 'Mixins',
    description: 'Reuse component logic with mixins — the classic approach before Composition API.',
    level: 'intermediate', duration: '30 min',
    objectives: ['Create and use mixins to share component logic', 'Understand mixin merge strategies for different option types', 'Recognize mixin limitations and when to prefer composables'],
    topics: [
      { id: 'mixin-basics', title: 'Mixin Basics', content: 'Mixins allow you to distribute reusable logic across components. A mixin is a plain JavaScript object containing component options like data methods computed and lifecycle hooks. When a component uses a mixin the mixin\'s options are merged into the component\'s own options. Data objects are recursively merged meaning the component\'s data takes precedence over the mixin\'s data for conflicting keys. Methods computed properties and lifecycle hooks with the same name are merged into an array and all run with mixin hooks executing first. You use mixins by adding a mixins array to the component definition. Mixins were the primary code reuse mechanism in Vue 2 and are still supported in Vue 3 for backward compatibility. However the Composition API with composables is now the preferred approach because it provides clearer dependency tracking and avoids the implicit merging problems of mixins.' },
      { id: 'mixin-merge', title: 'Option Merge Strategies', content: 'Vue uses specific strategies when merging mixin options with component options. Data objects are recursively merged with component values winning conflicts. Lifecycle hooks are merged into arrays where mixin hooks run before component hooks. For hooks like created both the mixin and component created hooks execute with the mixin first. Methods hooks and computed properties are merged into a single object where the component version takes precedence for name conflicts. The component option replaces the mixin option for string values like template and methods. Arrays like directives and components are concatenated. Options with array merge strategies like beforeCreate and created merge both the mixin and component values into arrays. Custom merge strategies can be defined per-option using app.config.optionMergeStrategies. Understanding merge behavior prevents subtle bugs when mixin options conflict with component options especially for data properties with the same name.' },
      { id: 'mixin-limitations', title: 'Mixin Limitations', content: 'Mixins have several known limitations that make composables preferable. The implicit nature of mixins makes it hard to trace where a property or method comes from. When reading a component you cannot tell which data properties methods or computed values come from the mixin without checking the mixin definition. Name collisions between mixins or between a mixin and the component can cause unexpected behavior where one option silently overrides another. Mixins share namespace which means two mixins providing data with the same key conflict. The Composition API solves these problems with composables that explicitly declare their dependencies and return values. Composables make dependencies visible in the import and destructuring statements. Mixins also make TypeScript integration harder because type inference does not work well across merged options. For new projects prefer composables over mixins. Use mixins only when maintaining legacy Vue 2 code or when building libraries that need backward compatibility.' }
    ],
    quiz: [
      { id: 1, question: 'How are lifecycle hooks merged when a mixin and component both define the same hook?', options: ['Only the component hook runs', 'Only the mixin hook runs', 'Both run in an array with the mixin first', 'They override each other randomly'], correctIndex: 2, explanation: 'Lifecycle hooks from mixins and components are merged into arrays. Mixin hooks execute first followed by the component hook.' },
      { id: 2, question: 'What is a major limitation of mixins compared to composables?', options: ['Mixins cannot use lifecycle hooks', 'Mixins make it hard to trace where properties come from due to implicit merging', 'Mixins are slower at runtime', 'Mixins cannot share state'], correctIndex: 1, explanation: 'Mixins implicitly merge options making it difficult to determine which properties and methods come from which source. Composables make dependencies explicit.' },
      { id: 3, question: 'When should you prefer composables over mixins?', options: ['In Vue 2 projects only', 'In all new Vue 3 projects for clearer dependency tracking', 'Only for complex applications', 'Never — mixins are always preferred'], correctIndex: 1, explanation: 'In new Vue 3 projects composables are preferred because they provide explicit dependency tracking, better TypeScript support, and avoid implicit merging issues.' }
    ]
  },
  {
    id: 525, slug: 'vue-filters', title: 'Filters',
    description: 'Format and transform data in templates using Vue filters (Options API only).',
    level: 'beginner', duration: '25 min',
    objectives: ['Define and use global and local filters', 'Apply pipe syntax for inline data transformation', 'Understand why filters were removed in Vue 3 and replacements'],
    topics: [
      { id: 'filter-basics', title: 'Filter Basics', content: 'Filters were a feature in Vue 1 and 2 that allowed you to apply text formatting and data transformation directly in template expressions using the pipe character. You define a filter as a function that takes a value and optional arguments and returns the transformed result. Global filters are registered with Vue.filter and are available in all templates. Local filters are defined in the filters option of a component and are only available in that component. Filters chain together so you can write multiple transformations in sequence. Each filter receives the output of the previous one as its first argument. Filters were popular for formatting dates currency and text transformations. They provided a clean declarative syntax in templates without cluttering component logic. However filters had limitations with reactivity integration TypeScript support and the Composition API which led to their removal in Vue 3.' },
      { id: 'filter-usage', title: 'Using Filters', content: 'In Vue 2 you use filters with the pipe operator in template mustache interpolations and v-bind expressions. A global filter named capitalize transforms a string by returning the first letter uppercase. In the template you write message | capitalize and Vue passes the message value through the filter function. Chaining multiple filters like message | capitalize | reverse applies each transformation in sequence. Filters can accept additional arguments after the first like currency amount currencySymbol. This syntax reads cleanly in templates and keeps formatting logic separate from component behavior. For local filters defined in the filters option the same pipe syntax applies but the filter is only available in that component. Filters cannot be used in script sections or JavaScript expressions outside templates. They are purely a template-level formatting tool which limits their flexibility compared to computed properties or methods.' },
      { id: 'filters-removal', title: 'Filters Removed in Vue 3', content: 'Vue 3 removed filters entirely in favor of methods and computed properties. The main reasons were poor TypeScript integration since filter types could not be inferred correctly, inability to work with the Composition API, and confusing syntax that looked different from standard JavaScript. The pipe operator also confused developers coming from other frameworks. The recommended migration path is to replace filters with methods or computed properties. A capitalize filter becomes a capitalize method or a computed property. For template-only formatting use a method call directly in the template expression. For reusable formatting logic create a utility function and import it into components. Libraries like vue-filter-date-fns and date-fns provide formatting utilities as plain functions. The computed property approach is preferred for derived values that should cache while methods work well for formatting on demand. This change aligns Vue with standard JavaScript patterns and improves TypeScript support.' }
    ],
    quiz: [
      { id: 1, question: 'What syntax did Vue filters use in templates?', options: ['Dot notation', 'Pipe operator', 'Arrow function', 'Template literal'], correctIndex: 1, explanation: 'Filters used the pipe operator | in templates like message | capitalize to apply transformations.' },
      { id: 2, question: 'Why were filters removed in Vue 3?', options: ['They were too slow', 'Poor TypeScript integration and incompatibility with Composition API', 'They did not work with v-bind', 'They were redundant with v-model'], correctIndex: 1, explanation: 'Filters had poor TypeScript support and could not work with the Composition API, leading to their removal in Vue 3.' },
      { id: 3, question: 'What is the recommended replacement for filters in Vue 3?', options: ['Computed properties and methods', 'Custom directives', 'Plugins', 'Provide/inject'], correctIndex: 0, explanation: 'Methods and computed properties replace filters by providing the same data transformation capability with better TypeScript and Composition API support.' }
    ]
  },
  {
    id: 526, slug: 'vue-transition', title: 'Vue Transitions',
    description: 'Add enter and leave animations to elements and components using Vue\'s built-in Transition component.',
    level: 'intermediate', duration: '35 min',
    objectives: ['Apply CSS transitions and animations with the Transition component', 'Use transition classes and JavaScript hooks for custom animations', 'Configure transition modes and appear behavior'],
    topics: [
      { id: 'transition-basics', title: 'Transition Basics', content: 'Vue provides a built-in Transition component that wraps elements to apply enter and leave animations. When you wrap an element with Transition and toggle its visibility with v-if or v-show Vue automatically adds and removes CSS transition classes during the animation. The transition enters by applying v-enter-from followed by v-enter-active and then v-enter-to. On leave it applies v-leave-from followed by v-leave-active and then v-leave-to. You define these classes in your CSS to create the animation effect. The enter and leave transitions can have different durations easing curves and properties. Transitions work with a single root element inside the Transition wrapper. The Transition component does not render a DOM element itself — it only manages the animation lifecycle of its child. Named transitions let you define multiple animations by giving the Transition a name prop and using the name as a CSS class prefix.' },
      { id: 'transition-classes', title: 'Transition CSS Classes', content: 'Vue defines six CSS classes for transitions that map to the animation phases. v-enter-from is the starting state before the element enters. v-enter-active is the active state during the entire enter phase where you define the transition property duration and easing. v-enter-to is the ending state after the element has entered. v-leave-from is the starting state when the element begins to leave. v-leave-active is the active state during the leave phase. v-leave-to is the final state after the element has left. For named transitions the v prefix is replaced with the name. Custom class names can be specified using the enter-active-class leave-active-class and other props. This is useful for integrating with third-party animation libraries like Animate.css. The appear prop makes the transition play on initial render. The css prop controls whether Vue waits for CSS transitions or starts JavaScript hooks immediately.' },
      { id: 'transition-modes', title: 'Transition Modes', content: 'When transitioning between two elements like with v-if and v-else you need to control how the enter and leave animations overlap. Without a mode both elements animate simultaneously which can cause layout issues. Vue provides three transition modes. The out-in mode completes the leave animation of the old element before starting the enter animation of the new element. The in-out mode starts the new element entering first then the old one leaves. The default mode runs enter and leave simultaneously. Out-in is the most commonly used mode because it prevents layout shifts and provides a clean transition between states. You set the mode attribute on the Transition component like mode="out-in". For JavaScript hooks the mode controls the order in which the hook callbacks fire. JavaScript transitions give full control using the beforeEnter enter afterEnter beforeLeave leave and afterLeave hooks which receive the DOM element and a done callback.' }
    ],
    quiz: [
      { id: 1, question: 'What CSS class is applied during the entire enter phase of a transition?', options: ['v-enter-from', 'v-enter-active', 'v-enter-to', 'v-leave-active'], correctIndex: 1, explanation: 'v-enter-active is applied during the entire enter phase and is where you define the transition duration, property, and easing.' },
      { id: 2, question: 'What does the out-in transition mode do?', options: ['Runs enter and leave simultaneously', 'Completes leave before starting enter', 'Starts enter before completing leave', 'Skips the leave animation'], correctIndex: 1, explanation: 'The out-in mode finishes the leave animation of the current element before starting the enter animation of the new element.' },
      { id: 3, question: 'What does the appear prop do on a Transition component?', options: ['It shows the element permanently', 'It plays the transition on initial render', 'It disables the transition', 'It triggers the transition on hover'], correctIndex: 1, explanation: 'The appear prop makes the enter transition play when the element is first rendered rather than only on subsequent show/hide toggles.' }
    ]
  },
  {
    id: 527, slug: 'vue-transition-group', title: 'TransitionGroup',
    description: 'Animate list enter leave and reorder operations using Vue\'s TransitionGroup component.',
    level: 'intermediate', duration: '30 min',
    objectives: ['Apply animations to list items using TransitionGroup', 'Handle list reorder and move animations', 'Configure staggered animations for lists'],
    topics: [
      { id: 'transition-group-basics', title: 'TransitionGroup Basics', content: 'TransitionGroup is a built-in component for animating the enter leave and reorder of list items. Unlike Transition which wraps a single element TransitionGroup renders a real DOM element (span by default) and manages animations for each child item. You use it by wrapping v-for rendered items with TransitionGroup. Each item gets its own enter and leave transition based on the keyed list. When items are added Vue applies the enter transition to the new items. When items are removed Vue applies the leave transition before removing them from the DOM. TransitionGroup requires a unique key on each list item to track additions removals and moves. Without proper keys Vue cannot determine which items moved and animations break. The tag prop specifies which HTML element wraps the list. CSS classes follow the same naming convention as Transition but prefixed with the name you provide.' },
      { id: 'list-animations', title: 'List Animations', content: 'TransitionGroup supports three types of list animations. Enter transitions animate newly added items appearing in the list. Leave transitions animate removed items disappearing before DOM removal. Move transitions animate existing items sliding to new positions when other items are added or removed. Move animations are triggered automatically when the order of keyed items changes. You apply move classes using the CSS transition property on the v-move class which is applied to all items when their position changes. Staggered animations create a cascading effect where each item animates with a small delay. You can implement stagger effects using CSS transition-delay calculated from the item index. JavaScript hooks on TransitionGroup receive an additional index parameter for stagger calculations. For smooth move animations ensure all items have the same height or use a fixed height. TransitionGroup also supports the mode attribute for controlling enter and leave overlap.' },
      { id: 'transition-group-performance', title: 'Performance Considerations', content: 'Animating large lists with TransitionGroup can be performance-intensive. Limit animations to visible items using v-show or virtual scrolling for very long lists. Use CSS transforms instead of layout-triggering properties like top or left to avoid repaints. The will-change CSS property hints to the browser that an element will animate helping with GPU acceleration. Avoid animating every property — focus on opacity and transform for smooth lightweight animations. Stagger delays should be short (20-50ms) to maintain perceived responsiveness. For lists with hundreds of items consider reducing animation complexity or using simpler fade transitions. JavaScript hooks give you fine-grained control to skip animations for certain items. The leave animation should complete before DOM removal to prevent flickering. Test animations on low-end devices to ensure smooth performance. Use Vue Devtools to monitor component re-renders during list transitions and identify bottlenecks.' }
    ],
    quiz: [
      { id: 1, question: 'What is a required attribute for TransitionGroup children?', options: ['class', 'style', 'key', 'ref'], correctIndex: 2, explanation: 'Each child in a TransitionGroup must have a unique key so Vue can track additions, removals, and position changes.' },
      { id: 2, question: 'How do you animate items sliding to new positions in a list?', options: ['Using v-enter classes', 'Using v-move CSS class with transitions', 'Using v-leave classes', 'Using JavaScript hooks only'], correctIndex: 1, explanation: 'The v-move class is automatically applied when item positions change. Define transitions on v-move to animate the sliding effect.' },
      { id: 3, question: 'What is the default wrapper element rendered by TransitionGroup?', options: ['div', 'ul', 'span', 'It renders no wrapper element'], correctIndex: 2, explanation: 'TransitionGroup renders a span element by default. You can change this with the tag prop.' }
    ]
  },
  {
    id: 528, slug: 'vue-keep-alive', title: 'KeepAlive',
    description: 'Cache inactive component instances to preserve state and avoid re-rendering.',
    level: 'intermediate', duration: '25 min',
    objectives: ['Use KeepAlive to cache component state across route changes', 'Configure include and exclude to selectively cache components', 'Handle cached component lifecycle with activated and deactivated hooks'],
    topics: [
      { id: 'keep-alive-basics', title: 'KeepAlive Basics', content: 'KeepAlive is a built-in abstract component that caches its dynamic child components when they are toggled off. Without KeepAlive when you use v-if to switch between components Vue destroys the inactive component and creates a new one when it appears again. This means all state including form inputs scroll position and local variables is lost. KeepAlive wraps the dynamic component and maintains its instance in memory. When the component is toggled back on it is reactivated from cache with its state intact. KeepAlive is commonly used with dynamic components and Vue Router to preserve component state when navigating between routes. The component is deactivated (not destroyed) when hidden and activated when shown again. The deactivated and activated lifecycle hooks replace mounted and unmounted for cached components. This is useful for preserving user input in multi-step forms or keeping a chat scroll position when switching tabs.' },
      { id: 'include-exclude', title: 'Include and Exclude', content: 'KeepAlive provides include and exclude props to selectively cache components. The include prop is a string or array of component names that should be cached. Only components whose names match will be kept alive. The exclude prop is a string or array of component names that should never be cached. Components matching exclude are always destroyed when hidden. Both props accept comma-separated strings like include="Foo,Bar" or arrays like :include="[\'Foo\', \'Bar\']". Component names are matched against the name option in the component definition or the name property exported from a script setup component using defineOptions. If neither include nor exclude is specified all child components are cached. The max prop limits the number of cached instances — when the limit is exceeded the least recently used component is destroyed. This prevents unbounded memory growth in applications with many cached components.' },
      { id: 'keep-alive-hooks', title: 'Cached Component Lifecycle', content: 'Components inside KeepAlive have a different lifecycle than normal components. When a cached component is first mounted it goes through the standard lifecycle including onMounted. When it is hidden it does not unmount — instead it triggers onDeactivated which is the Composition API equivalent of the deactivated hook. The DOM is preserved in a hidden state. When the component is shown again it triggers onActivated without going through onMounted again. This cycle repeats every time the component is toggled. These hooks are the right place to manage behavior specific to cached components. For example pause timers or video playback in onDeactivated and resume them in onActivated. Fetch fresh data in onActivated if the cached data may be stale. Note that onUnmounted only fires when the component is permanently removed from the cache such as when KeepAlive itself unmounts or the component is evicted due to max limit.' }
    ],
    quiz: [
      { id: 1, question: 'What happens to a component\'s state when it is wrapped in KeepAlive and hidden?', options: ['The state is destroyed', 'The state is preserved in a deactivated state', 'The state is serialized to localStorage', 'The state is reset to initial values'], correctIndex: 1, explanation: 'KeepAlive preserves the component instance and its state in memory. The component is deactivated but not destroyed.' },
      { id: 2, question: 'What does the max prop on KeepAlive control?', options: ['Maximum number of render cycles', 'Maximum number of cached component instances', 'Maximum memory usage', 'Maximum component size'], correctIndex: 1, explanation: 'The max prop limits how many component instances are cached. When exceeded the least recently used component is destroyed.' },
      { id: 3, question: 'Which lifecycle hook fires when a cached component is shown again?', options: ['onMounted', 'onActivated', 'onCreated', 'onUpdated'], correctIndex: 1, explanation: 'onActivated fires when a cached component is reactivated. onMounted only fires on the initial mount.' }
    ]
  },
  {
    id: 529, slug: 'vue-teleport', title: 'Teleport',
    description: 'Render component content in a different DOM location using the Teleport component.',
    level: 'intermediate', duration: '25 min',
    objectives: ['Use Teleport to render content outside the component tree', 'Build accessible modals and tooltips with Teleport', 'Understand the relationship between teleported content and the component tree'],
    topics: [
      { id: 'teleport-basics', title: 'Teleport Basics', content: 'Teleport is a built-in component that renders its content in a different part of the DOM tree while keeping it logically connected to the parent component. The to prop specifies the target DOM element as a CSS selector like to="body" or to="#modal-container". The content maintains its Vue component instance including reactivity and event handling but its DOM nodes appear at the target location. This is essential for components like modals dropdowns and tooltips that need to escape their parent container\'s overflow hidden or z-index stacking context. Without Teleport you would need to manually manage DOM insertion and maintain event connections. Teleport preserves the parent-child relationship in the Vue component tree so provide and inject still work. Props and events between the parent and teleported content function normally. The Teleport component itself renders no DOM element — only its children are teleported.' },
      { id: 'teleport-usage', title: 'Teleport Usage', content: 'Teleport is commonly used for modals and dialogs that should overlay the entire page. You place the modal content inside Teleport to="body" so it appears at the top of the DOM regardless of where the triggering component lives. This prevents z-index issues caused by parent elements with position relative or transform properties. For tooltips Teleport moves the tooltip content to the body so it is not clipped by parent overflow. Multi-step wizards use Teleport to render navigation buttons in a fixed footer area while keeping the wizard logic in the component tree. You can have multiple Teleport components targeting the same element and their content is appended in order. Teleport works with dynamic to values so you can change the target at runtime. The disabled prop lets you conditionally disable teleporting rendering the content in its original DOM location. This is useful for responsive designs where you want different behavior at different screen sizes.' },
      { id: 'teleport-and-events', title: 'Teleport and Component Communication', content: 'Teleported content maintains full communication with its source component tree. Event handlers on teleported elements work normally because the Vue instance is still connected to the parent. Props passed to components inside Teleport update reactively. Provide and inject work across the teleport boundary so parent-provided values are accessible. The component tree shown in Vue Devtools reflects the logical structure not the physical DOM structure. This means the teleported component appears as a child of its Vue parent even though its DOM nodes are elsewhere. However there are considerations for event propagation. DOM events bubble through the physical DOM tree not the Vue component tree. If a click inside a teleported modal bubbles up to the body it might trigger unintended handlers. Use event.stopPropagation carefully or manage event handling through Vue events rather than DOM events. For keyboard events consider the focus trap — when a modal opens focus should move to the modal and return when it closes.' }
    ],
    quiz: [
      { id: 1, question: 'What does the to prop on Teleport specify?', options: ['The component to render', 'The target DOM element as a CSS selector', 'The event handler', 'The transition name'], correctIndex: 1, explanation: 'The to prop specifies a CSS selector for the target DOM element where the teleported content will be rendered.' },
      { id: 2, question: 'Why is Teleport useful for modals?', options: ['It makes modals faster', 'It renders modals outside their parent container avoiding z-index and overflow issues', 'It creates modals automatically', 'It handles modal state'], correctIndex: 1, explanation: 'Teleport renders modal content at the body level avoiding z-index stacking context and overflow hidden issues from parent containers.' },
      { id: 3, question: 'Do provide/inject values work across the Teleport boundary?', options: ['No, they are broken by teleporting', 'Yes, the logical component tree is preserved', 'Only if you re-provide them', 'Only with reactive values'], correctIndex: 1, explanation: 'Teleport preserves the logical Vue component tree so provide and inject continue to work across the teleport boundary.' }
    ]
  },
  {
    id: 530, slug: 'vue-suspense', title: 'Suspense',
    description: 'Handle asynchronous components gracefully with Vue\'s built-in Suspense component.',
    level: 'advanced', duration: '35 min',
    objectives: ['Use Suspense to manage async component loading states', 'Create async setup functions and async components', 'Implement fallback UI for loading states with error boundaries'],
    topics: [
      { id: 'suspense-basics', title: 'Suspense Basics', content: 'Suspense is an experimental built-in component that handles asynchronous dependencies in a declarative way. It shows a fallback UI while waiting for async operations to complete and then reveals the content. You place async components or components with async setup as children of Suspense. When all async dependencies resolve Suspense replaces the fallback with the actual content. The Suspense component has two slots — the default slot for the main content and the fallback slot for the loading state. A component with an async setup function is automatically treated as an async dependency. The async setup can perform API calls and return the result which becomes available to the template. Suspense tracks all async dependencies within its subtree and waits for all of them before showing the content. This eliminates the need for manual loading state management in individual components.' },
      { id: 'async-components', title: 'Async Components', content: 'Async components are components whose definition is loaded lazily using defineAsyncComponent. When the component is first rendered Vue shows the fallback and starts loading the component definition asynchronously. Once loaded Vue renders the actual component. You can configure the loading component (shown during load) the error component (shown if loading fails) and timeout settings. Combined with Suspense async components provide a seamless loading experience. In the Composition API an async component uses an async setup function that awaits data before returning. Suspense detects the promise returned by setup and waits for it to resolve. During this time the fallback slot content is displayed. You can nest Suspense boundaries for different loading levels. Error handling uses onErrorCaptured to catch errors from async components. This pattern eliminates waterfall loading where components wait for each other sequentially.' },
      { id: 'suspense-patterns', title: 'Suspense Patterns', content: 'Suspense works well with route-level code splitting where each page is an async component. The router renders the async page component inside Suspense which shows a loading indicator while the chunk loads. For data fetching async setup in each component fetches its own data. Suspense waits for all component data to load before showing the page. This eliminates loading waterfalls because all components start fetching simultaneously. Error boundaries use onErrorCaptured to catch and display errors from any async component in the subtree. A common pattern is wrapping the entire app router view in Suspense with a global loading component. For partial loading use multiple Suspense boundaries — one for the navigation (always visible) and one for the page content (shows loading). While Suspense is still experimental it is widely used in production with Nuxt 3. Keep in mind that Suspense works best when async operations are parallel not sequential.' }
    ],
    quiz: [
      { id: 1, question: 'What does Suspense do while waiting for async dependencies?', options: ['Shows nothing', 'Displays the fallback slot content', 'Throws an error', 'Renders the default slot immediately'], correctIndex: 1, explanation: 'Suspense shows the fallback slot content while all async dependencies within its subtree are resolving.' },
      { id: 2, question: 'How does Suspense detect async components?', options: ['By checking for loading prop', 'By detecting promises returned from async setup functions', 'By analyzing component file size', 'By checking the component name'], correctIndex: 1, explanation: 'Suspense detects promises returned from async setup functions and waits for them to resolve before rendering the content.' },
      { id: 3, question: 'What happens when an async component inside Suspense fails to load?', options: ['The fallback is shown permanently', 'An error is thrown that can be caught by onErrorCaptured', 'The app crashes', 'The component is retried automatically'], correctIndex: 1, explanation: 'Errors from async components propagate up and can be caught by onErrorCaptured to display error UI.' }
    ]
  },
  {
    id: 531, slug: 'vue-render-function', title: 'Render Function',
    description: 'Create components programmatically using render functions instead of templates.',
    level: 'advanced', duration: '35 min',
    objectives: ['Create components using the render function and h() helper', 'Understand virtual DOM node creation with h()', 'Use render functions for dynamic component structures'],
    topics: [
      { id: 'render-function-basics', title: 'Render Function Basics', content: 'Render functions let you create component output programmatically in JavaScript instead of using template syntax. A render function returns virtual DOM nodes created with the h() helper function. The h function takes three arguments — the element type (a string for HTML or a component), props (attributes and event listeners), and children (text elements or nested vnodes). In Vue 3 you import h from vue and call it inside a setup function that returns the render function or inside the render option. Render functions are powerful for components that need highly dynamic structures where templates would be verbose or impossible. You can use full JavaScript logic including conditionals loops and variable assignments to build the virtual DOM tree. The render function replaces the template compilation — instead of parsing a template string Vue calls your function directly to produce the vnode tree.' },
      { id: 'h-function', title: 'The h() Function', content: 'The h() function (short for hyperscript) creates virtual DOM nodes. The first argument is the element type — use a string like div or span for HTML elements or a component reference for Vue components. The second argument is a props object containing attributes class style and event listeners prefixed with on like onClick. The third argument is the children which can be a string for text content an array of vnodes for nested elements or null for no children. You can create complex nested structures by calling h multiple times and nesting the results. The vnodes returned by h are plain JavaScript objects describing what should be rendered. Vue compares the new vnodes with the previous ones to determine minimal DOM updates. Dynamic props can be spread using object spread syntax. Slots are accessed through the context object in the render function allowing you to render parent-provided slot content.' },
      { id: 'render-function-use-cases', title: 'Use Cases for Render Functions', content: 'Render functions excel in several scenarios. Component libraries that generate markup programmatically based on configuration objects use render functions to map data to DOM structure. Highly dynamic components like tree views or data grids where each cell structure varies based on data benefit from the flexibility of JavaScript over templates. Higher-order components that wrap other components and modify their props or events are cleaner with render functions. Wrapper components that delegate rendering to a child while adding behavior use render functions to pass through slots and props. Render functions also work well for components that adapt their output based on runtime conditions that are too complex for template directives. However render functions are harder to read and maintain than templates. Use them when templates cannot express the required logic. Most components should use templates for clarity and better tooling support including IDE autocomplete and Vue Devtools.' }
    ],
    quiz: [
      { id: 1, question: 'What does the h() function return?', options: ['A DOM element', 'A virtual DOM node', 'A template string', 'A component instance'], correctIndex: 1, explanation: 'h() returns a virtual DOM node (vnode) which is a plain JavaScript object describing what should be rendered.' },
      { id: 2, question: 'What are the three arguments of the h() function?', options: ['name value callback', 'type props children', 'component state template', 'selector attribute content'], correctIndex: 1, explanation: 'h() takes the element type, a props object, and children (text, vnodes, or null) as its three arguments.' },
      { id: 3, question: 'When should you prefer render functions over templates?', options: ['Always for better performance', 'When the component structure is highly dynamic and cannot be expressed in templates', 'When you want better IDE support', 'When the component is simple'], correctIndex: 1, explanation: 'Render functions are best for highly dynamic components where template syntax is too restrictive. Templates are preferred for readability and tooling support.' }
    ]
  },
  {
    id: 532, slug: 'vue-jsx', title: 'JSX in Vue',
    description: 'Write Vue render functions using JSX syntax for a more familiar and readable approach.',
    level: 'advanced', duration: '30 min',
    objectives: ['Use JSX syntax in Vue components for cleaner render functions', 'Configure Vite and TypeScript for JSX support', 'Understand JSX differences between Vue and React'],
    topics: [
      { id: 'vue-jsx-basics', title: 'JSX Basics in Vue', content: 'JSX is a syntax extension that lets you write HTML-like code inside JavaScript. While Vue typically uses template syntax you can use JSX in render functions for a more readable approach. In Vue JSX you write element structures using angle brackets with JavaScript expressions in curly braces. JSX compiles to h() function calls which produce virtual DOM nodes. To use JSX in Vue you need the @vitejs/plugin-vue-jsx plugin for Vite or the appropriate Babel plugin for Webpack. The plugin transforms JSX into Vue-compatible render function calls. JSX in Vue differs from React JSX in several ways — Vue uses class instead of className, event listeners use on-prefix like onClick, and you can use v-model and v-show directives directly in JSX. Vue JSX supports the spread operator for props and allows full JavaScript expressions including conditionals and loops.' },
      { id: 'jsx-configuration', title: 'JSX Configuration', content: 'Setting up JSX in a Vue project requires installing the vue-jsx plugin. For Vite projects install @vitejs/plugin-vue-jsx and add it to the Vite config plugins array. For TypeScript add the jsxImportSource option to your tsconfig for automatic JSX type checking. The plugin handles the transformation of JSX syntax into Vue render function calls. You can customize the JSX pragma to use a different function than the default h. In script setup components you need to configure the defineProps and defineEmits macros to work with JSX. The plugin provides defineComponent for type-safe component definitions when using JSX. Hot module replacement works with JSX components. Named slots in JSX use the $slots object. Event modifiers like .prevent and .stop are applied directly on the JSX attributes. Fragment support allows returning multiple elements without a wrapper.' },
      { id: 'jsx-patterns', title: 'JSX Patterns', content: 'JSX enables patterns that are difficult with templates. Conditional rendering uses JavaScript ternary operators and logical AND directly in JSX making complex conditions more readable. List rendering uses array map methods instead of v-for which some developers find more natural. Higher-order components wrap child components and add behavior by passing modified props or event handlers. Renderless components return JSX without any DOM elements passing their state to scoped slots via render props. Component factories create different component configurations from data objects. Dynamic component selection uses the component variable directly in JSX tag position. JSX works well with TypeScript giving you full type inference for props events and slots. When using JSX remember that Vue class bindings use class not className. Style bindings accept objects. Event listeners follow the onEvent pattern. Slots are accessed as functions on the context object passed to the render function.' }
    ],
    quiz: [
      { id: 1, question: 'What plugin is needed for JSX support in Vue with Vite?', options: ['@vitejs/plugin-vue', '@vitejs/plugin-vue-jsx', 'vue-jsx-plugin', 'vite-plugin-jsx'], correctIndex: 1, explanation: 'The @vitejs/plugin-vue-jsx plugin transforms JSX syntax into Vue-compatible render function calls.' },
      { id: 2, question: 'What attribute name do you use for CSS classes in Vue JSX?', options: ['className', 'class', 'cssClass', 'classList'], correctIndex: 1, explanation: 'Vue JSX uses class (not className which is the React convention) for CSS class bindings.' },
      { id: 3, question: 'How do you conditionally render elements in Vue JSX?', options: ['Using v-if directive', 'Using JavaScript ternary operators and logical expressions', 'Using ng-if', 'Using template literals'], correctIndex: 1, explanation: 'JSX allows full JavaScript expressions including ternary operators and logical AND for conditional rendering directly in the syntax.' }
    ]
  },
  {
    id: 533, slug: 'vue-composables', title: 'Composables',
    description: 'Extract and reuse stateful logic using Vue composable functions.',
    level: 'intermediate', duration: '35 min',
    objectives: ['Create composable functions that encapsulate reusable logic', 'Understand the naming convention and return value patterns', 'Compose multiple composables to build complex behavior'],
    topics: [
      { id: 'composable-basics', title: 'Composable Basics', content: 'Composables are functions that encapsulate stateful logic for reuse across components. They are the Vue 3 Composition API replacement for mixins. A composable is a plain JavaScript function that uses reactive APIs like ref and reactive internally. It typically takes reactive inputs as arguments and returns reactive values and methods. The naming convention is to prefix composable functions with use like useCounter useFetch or useLocalStorage. Composables can call other composables creating a composition hierarchy. Unlike mixins composables make dependencies explicit — you can see exactly what a component uses by looking at its imports and destructuring. Composables return plain JavaScript objects so they work with tree-shaking and TypeScript naturally. They execute in the component context so lifecycle hooks and provide/inject work as expected. The composable function should always call reactive APIs at the top level not inside callbacks to maintain reactivity.' },
      { id: 'composable-patterns', title: 'Composable Patterns', content: 'Common composable patterns include state management data fetching DOM manipulation and event handling. A useCounter composable encapsulates count state with increment decrement and reset methods. A useFetch composable handles loading states errors and data from an API endpoint accepting a reactive URL ref. A useMousePosition composable tracks mouse coordinates by adding event listeners in onMounted and cleaning them up in onUnmounted. A useLocalStorage composable synchronizes reactive state with localStorage using watchers. Composables that accept reactive inputs use watch or watchEffect to react to changes. The input parameter is often a getter function or ref to maintain reactivity. For composable functions that need cleanup return a cleanup function or use onUnmounted. Composables can return computed values methods refs and reactive objects. Group related composables in a composables directory organized by domain like composables/auth composables/api and composables/ui.' },
      { id: 'composing-composables', title: 'Composing Composables', content: 'One of the strongest aspects of composables is that they compose naturally. A complex feature can be built by combining simpler composables. A useAuth composable might combine useLocalStorage for persisting tokens with useFetch for API calls and useInterval for token refresh. A useInfiniteScroll composable might use useEventListener for scroll events useIntersectionObserver for detection and useFetch for data loading. When composing composables each one manages its own state and lifecycle independently. They do not interfere with each other because they are just function calls. You can pass refs between composables and they stay reactive. For composables that share state pattern them with a factory function that creates a fresh instance each time like useCounter which returns new state per call. For singleton state like global stores use module-level refs outside the function. Composables make complex features testable by testing each composable independently then testing the composition.' }
    ],
    quiz: [
      { id: 1, question: 'What is the naming convention for composable functions?', options: ['createX or makeX', 'useX prefix', 'getX or fetchX', 'initX'], correctIndex: 1, explanation: 'Composables follow the useX naming convention like useCounter useFetch to indicate they are reactive composition functions.' },
      { id: 2, question: 'What makes composables better than mixins for code reuse?', options: ['Composables are faster', 'They make dependencies explicit and support tree-shaking and TypeScript', 'They use less memory', 'They work in both Vue 2 and Vue 3'], correctIndex: 1, explanation: 'Composables explicitly declare their inputs and outputs, support tree-shaking, work with TypeScript, and avoid the implicit merging problems of mixins.' },
      { id: 3, question: 'How do you ensure a composable maintains reactivity for its inputs?', options: ['By using props', 'By accepting refs or getter functions as arguments', 'By using provide/inject', 'By using global state'], correctIndex: 1, explanation: 'Composables accept refs or getter functions as arguments so they can reactively respond to input changes using watch or watchEffect.' }
    ]
  },
  {
    id: 534, slug: 'vue-use-magic', title: 'VueUse',
    description: 'Supercharge development with the VueUse collection of composable utilities.',
    level: 'intermediate', duration: '30 min',
    objectives: ['Use VueUse composables for common UI and utility patterns', 'Understand the organization and categories of VueUse functions', 'Integrate VueUse composables into real-world component scenarios'],
    topics: [
      { id: 'vueuse-overview', title: 'VueUse Overview', content: 'VueUse is a collection of essential Vue Composition API utilities with over 200 composable functions. It provides ready-made composables for sensors (mouse position window size battery status), UI (dark mode infinite scroll gestures), state (localStorage sessionStorage), animations ( transitions) and more. Each composable handles setup cleanup and reactivity automatically. Install it with npm install @vueuse/core and import individual functions. VueUse composables follow consistent patterns — they accept reactive inputs and return reactive outputs with automatic cleanup. The library is tree-shakable so you only pay for what you use. VueUse works with Vue 3 and has a companion package @vueuse/integrations for combining with other libraries. The documentation at vueuse.org includes interactive demos and TypeScript types for every function. VueUse saves significant development time by providing battle-tested implementations of common patterns.' },
      { id: 'vueuse-categories', title: 'VueUse Categories', content: 'VueUse organizes composables into categories for easy discovery. Sensors include useMouseInElement useWindowSize useDeviceOrientation and useBattery for reading device and input state. Animation composables like useTransition and useParallax provide smooth value interpolation and visual effects. State composables like useLocalStorage useSessionStorage and useCookie persist reactive state across page loads. UI composables include useDark useToggle useInfiniteScroll and useVirtualList for common interface patterns. Network composables like useOnline and useFetch handle connectivity detection and HTTP requests. Component composables like onClickOutside onKeyStroke and useElementVisibility handle DOM interactions. Utility composables include useDebounceFn useThrottleFn useIntervalFn and useTimestamp for time-based operations. Each category addresses a specific development need. You do not need to import all of VueUse — only import the specific functions you need for optimal bundle size.' },
      { id: 'vueuse-integration', title: 'Integrating VueUse', content: 'Integrating VueUse into a project is straightforward. Install the core package and import individual composables in your components. For example import { useMouse } from @vueuse/core and call it in setup to track mouse position. VueUse composables return reactive values that you can bind directly to templates. For dark mode use useDark and useToggle which handle system preference detection and localStorage persistence automatically. useInfiniteScroll detects when the user scrolls to the bottom and triggers a callback for loading more data. useTransition animates a value from one number to another with configurable easing. VueUse also provides integrations with other libraries through @vueuse/integrations. This includes useAxios for HTTP requests useSWRV for data fetching and usei18n for internationalization. The composables work seamlessly with other Composition API code and can be combined into custom composables for your specific needs.' }
    ],
    quiz: [
      { id: 1, question: 'How do you install VueUse?', options: ['It is built into Vue', 'npm install @vueuse/core', 'npm install vueuse', 'yarn add @vue/vueuse'], correctIndex: 1, explanation: 'VueUse is installed as a separate package via npm install @vueuse/core and individual functions are imported as needed.' },
      { id: 2, question: 'What is a key benefit of VueUse composables?', options: ['They replace the Vue router', 'They handle setup and cleanup automatically with tree-shaking support', 'They are required for Vue 3', 'They replace Pinia'], correctIndex: 1, explanation: 'VueUse composables automatically handle lifecycle setup and cleanup and are tree-shakable so unused functions do not increase bundle size.' },
      { id: 3, question: 'Which VueUse composable provides dark mode functionality?', options: ['useTheme', 'useDark and useToggle', 'useColorScheme', 'useMode'], correctIndex: 1, explanation: 'useDark detects system preference and toggles the dark class on the html element while useToggle provides a reactive toggle method.' }
    ]
  },
  {
    id: 535, slug: 'vue-form-validation', title: 'Form Validation',
    description: 'Build robust form validation in Vue using reactive forms, Vuelidate, and VeeValidate.',
    level: 'intermediate', duration: '35 min',
    objectives: ['Implement form validation with VeeValidate and Zod schemas', 'Handle validation errors and display user-friendly messages', 'Create custom validators and async validation patterns'],
    topics: [
      { id: 'validation-approaches', title: 'Validation Approaches', content: 'Vue offers several approaches to form validation. The simplest is manual validation using computed properties and watchers to check input values against rules. This works for small forms but becomes verbose for complex ones. VeeValidate is a popular library that provides declarative validation using component-based or composition API patterns. It supports schema-based validation with Zod or Yup. Vuelidate is another library that uses a validation schema defined alongside your reactive data. For simple cases you can use v-model with a computed setter that validates on input. The Composition API approach uses composables like useForm and useField from VeeValidate which provide reactive validation state errors and touched flags. For maximum control build a custom validation composable that accepts rules as configuration. The choice depends on form complexity and whether you want library-managed or hand-rolled validation.' },
      { id: 'vee-validate', title: 'VeeValidate', content: 'VeeValidate is the most popular form validation library for Vue 3. It offers two API styles — component-based using Field and Form components and composable-based using useForm and useField. The composable API integrates naturally with the Composition API. Define a validation schema using Zod or Yup describing the expected shape and rules for each field. Pass the schema to useForm which returns reactive state for each field including value error touched and meta information. useField creates individual field instances with their own validation state. VeeValidate handles form-level validation submit prevention dirty state tracking and error message display. The ErrorMessage component displays field-specific errors. Custom validators are functions that receive the value and return true or an error message. Async validators support server-side validation by returning a promise. VeeValidate also provides i18n support for translating error messages.' },
      { id: 'validation-patterns', title: 'Validation Patterns', content: 'Several validation patterns improve user experience. Validate on blur (when the user leaves a field) to avoid showing errors while the user is still typing. Validate on submit to catch all errors at once when the form is submitted. Real-time validation gives immediate feedback but can be annoying if errors appear too early. A good UX combines these — show errors after first blur then validate in real-time. Cross-field validation checks relationships between fields like ensuring a password confirmation matches. VeeValidate handles this with the confirmed validation rule. Conditional validation applies rules based on other field values like requiring a phone number only when a specific checkbox is checked. Server-side validation checks values against backend rules and displays server-returned errors. Always provide clear error messages that explain what is wrong and how to fix it. Use red text borders and icons to draw attention to invalid fields.' }
    ],
    quiz: [
      { id: 1, question: 'What is VeeValidate?', options: ['A CSS framework for forms', 'A form validation library for Vue with schema-based validation support', 'A state management solution', 'A testing utility for forms'], correctIndex: 1, explanation: 'VeeValidate is a comprehensive form validation library that supports schema-based validation with Zod or Yup and integrates with Vue 3 Composition API.' },
      { id: 2, question: 'When should you show validation errors for the best user experience?', options: ['Immediately on page load', 'After the first blur then in real-time', 'Only on form submission', 'Never show errors'], correctIndex: 1, explanation: 'Show errors after the first blur (when the user leaves the field) then validate in real-time to balance feedback with avoiding premature error display.' },
      { id: 3, question: 'How do you perform cross-field validation?', options: ['Using separate watchers for each field', 'Using rules like confirmed that reference another field', 'By comparing DOM values manually', 'Cross-field validation is not possible'], correctIndex: 1, explanation: 'VeeValidate provides rules like confirmed that compare one field value against another for cases like password confirmation matching.' }
    ]
  },
  {
    id: 536, slug: 'vue-http-client', title: 'HTTP Client',
    description: 'Make HTTP requests in Vue using Fetch API, Axios, and VueUse composables.',
    level: 'intermediate', duration: '30 min',
    objectives: ['Set up an HTTP client with interceptors and base configuration', 'Handle loading states errors and response transformation', 'Implement request cancellation and retry patterns'],
    topics: [
      { id: 'http-setup', title: 'HTTP Client Setup', content: 'Setting up an HTTP client involves choosing between the native Fetch API and libraries like Axios. The Fetch API is built into browsers and requires no additional dependencies. It returns Promises and uses the Response interface for handling results. Axios is a popular library that provides request and response interceptors automatic JSON parsing and better error handling. To set up Axios create an instance with a base URL default headers and timeout. Interceptors run before every request to add authentication tokens or logging. Response interceptors transform data or handle specific status codes globally. For Vue integration create a composable like useHttpClient that wraps the HTTP client and provides reactive loading and error state. The composable pattern makes HTTP calls reactive and integrates with the component lifecycle. Place your HTTP configuration in a services directory with separate files for different API domains.' },
      { id: 'request-patterns', title: 'Request Patterns', content: 'Common HTTP request patterns in Vue include GET for fetching data POST for creating resources PUT for updating and DELETE for removing. Each pattern should handle loading state, error state, and response data reactively. Use ref for loading and error flags and a ref or reactive for the response data. Request cancellation prevents stale responses from overwriting newer data. With Axios use AbortController to cancel requests when a component unmounts or when a new request replaces the previous one. With Fetch pass an AbortSignal to the signal option. Retry patterns automatically retry failed requests with exponential backoff. Implement retry logic in a wrapper function that catches errors and retries up to a configurable limit. Optimistic updates immediately update the UI before the server confirms the change and roll back on error. This creates a snappy user experience while maintaining data consistency.' },
      { id: 'error-handling', title: 'HTTP Error Handling', content: 'Proper error handling for HTTP requests involves multiple layers. Network errors (no connection) need different handling than HTTP errors (4xx 5xx responses). Axios throws for non-2xx status codes while Fetch only throws for network failures. For Axios use try-catch and check error.response.status for specific handling. For Fetch check response.ok before parsing. Global error handlers intercept all HTTP errors and can show toast notifications or redirect to login on 401. Per-request error handling provides context-specific messages. Loading states prevent multiple simultaneous requests and show progress indicators. Timeout handling detects when requests take too long and provides feedback. For form submissions display validation errors from 422 responses alongside the relevant form fields. Always provide a way for users to retry failed requests. Log errors to monitoring services for debugging. Consider using a retry library for transient failures like network glitches.' }
    ],
    quiz: [
      { id: 1, question: 'What advantage does Axios have over the native Fetch API?', options: ['It is faster', 'It provides request and response interceptors and automatic JSON parsing', 'It uses less memory', 'It works offline'], correctIndex: 1, explanation: 'Axios provides interceptors for global request/response handling, automatic JSON parsing, and better error handling compared to the native Fetch API.' },
      { id: 2, question: 'How do you prevent stale responses from overwriting newer data?', options: ['Use local storage', 'Use AbortController to cancel previous requests', 'Use setTimeout', 'Disable loading states'], correctIndex: 1, explanation: 'AbortController cancels previous requests when a new one is initiated, preventing stale responses from overwriting newer data.' },
      { id: 3, question: 'What is an optimistic update?', options: ['Updating the server before the client', 'Immediately updating the UI before server confirmation and rolling back on error', 'Updating all components at once', 'Pre-updating cached data'], correctIndex: 1, explanation: 'Optimistic updates immediately change the UI to reflect the expected result, then roll back if the server returns an error.' }
    ]
  },
  {
    id: 537, slug: 'vue-pinia-deep', title: 'Pinia Deep Dive',
    description: 'Master Pinia state management with advanced patterns, plugins, and server-side integration.',
    level: 'intermediate', duration: '40 min',
    objectives: ['Create stores with getters actions and state in Pinia', 'Use store composition and plugin patterns', 'Implement SSR-compatible state hydration with Pinia'],
    topics: [
      { id: 'pinia-store-patterns', title: 'Store Patterns', content: 'Pinia stores are defined using defineStore which takes a store ID and either an Options API or Composition API setup function. The Options API style separates state (reactive properties), getters (computed values), and actions (methods). The Composition API style uses ref and computed for state and getters with plain functions for actions. Each style has tradeoffs — Options API is more structured while Composition API is more flexible and familiar to Composition API users. Store composition lets you call one store from another enabling modular state management. For example a useCartStore might call useUserStore to get the authenticated user for order placement. Getters in stores are computed values that derive from state. They cache results and update when dependencies change. Actions can be synchronous or asynchronous and can call other actions. Stores are automatically tree-shakable when using the setup function style.' },
      { id: 'pinia-plugins', title: 'Pinia Plugins', content: 'Pinia plugins extend store functionality by adding global properties methods or behavior. A plugin receives the Pinia instance and can intercept store creation using pinia.use(). Common plugins add persistence (saving state to localStorage), logging (tracking state changes), devtools integration, and undo/redo functionality. The persistence plugin pattern wraps each store creation to automatically save and restore state from storage. You can configure plugins per-store using a special option or apply them globally. Plugins can add new properties to stores by returning an object that gets merged with the store. The subscribe method on each store allows watching state changes for side effects. Plugins execute in order so the last plugin added runs first. For SSR be careful with plugins that access browser APIs — check for server-side rendering context before accessing window or document. Pinia plugins follow the same pattern as Vue plugins with install functions.' },
      { id: 'pinia-ssr', title: 'Pinia and SSR', content: 'Server-side rendering with Pinia requires serializing store state on the server and hydrating it on the client. During SSR Pinia stores are created fresh on each request with their initial state. After the server renders the page the store state is serialized and embedded in the HTML as a script tag. On the client Pinia reads this serialized state and hydrates the stores so the client-side application starts with the same state as the server render. Nuxt 3 handles this automatically — stores created during SSR are serialized and hydrated without extra configuration. For manual SSR setups use pinia.state.value to access the global state and JSON.stringify to serialize it. On the client pass the serialized state to createPinia before mounting the app. Ensure sensitive data like authentication tokens are not included in the serialized state that is exposed to the client. The hydration process prevents the client from fetching data that was already loaded on the server.' }
    ],
    quiz: [
      { id: 1, question: 'What are the two API styles for defining Pinia stores?', options: ['Class-based and function-based', 'Options API and Composition API (setup function)', 'Template-based and script-based', 'Module-based and class-based'], correctIndex: 1, explanation: 'Pinia supports both Options API style (with state getters actions) and Composition API style (using ref computed and plain functions).' },
      { id: 2, question: 'How do Pinia plugins extend store functionality?', options: ['By modifying the Vue prototype', 'By intercepting store creation and adding properties or methods', 'By rewriting store code', 'By generating store code'], correctIndex: 1, explanation: 'Pinia plugins intercept store creation via pinia.use() and can add new properties methods or modify existing behavior.' },
      { id: 3, question: 'What is the purpose of state hydration in SSR?', options: ['To speed up the server render', 'To transfer server-rendered state to the client so the app does not refetch data', 'To encrypt store data', 'To clear store state on navigation'], correctIndex: 1, explanation: 'State hydration transfers the server-rendered store state to the client so the client-side app starts with the same state without re-fetching data.' }
    ]
  },
  {
    id: 538, slug: 'vue-router-deep', title: 'Vue Router Deep Dive',
    description: 'Master Vue Router with dynamic routes nested layouts lazy loading and advanced navigation.',
    level: 'intermediate', duration: '40 min',
    objectives: ['Configure nested routes with dynamic parameters and wildcards', 'Implement route-based code splitting with lazy loading', 'Build complex layout systems using nested router views'],
    topics: [
      { id: 'route-configuration', title: 'Route Configuration', content: 'Vue Router routes are defined as an array of route objects passed to createRouter. Each route has a path component and optional properties. Dynamic segments use colon syntax like /user/:id where id is accessible via useRoute().params.id. Nested routes use the children property to define sub-routes under a parent. The parent component renders a router-view where child components appear. Catch-all routes use the wildcard syntax like /:pathMatch(.*) to handle unmatched URLs. Route aliases use the alias property to render the same component under multiple paths. Route props pass route parameters as component props instead of accessing them through useRoute. Named routes give routes a name for programmatic navigation. The beforeEnter guard on individual routes controls access per-route. Redirects use the redirect property to send users to a different path. Route meta lets you attach custom data like requiresAuth or title to routes.' },
      { id: 'lazy-loading', title: 'Route-Level Lazy Loading', content: 'Lazy loading splits your application into smaller chunks that load only when their route is visited. Instead of importing components directly use dynamic imports in the component property of your route definition. The import returns a Promise that resolves to the component module. Vue Router automatically wraps this in a Suspense-like mechanism showing nothing while loading. Named chunks use webpack magic comments or Vite import.meta.glob to group related routes into the same chunk. Prefetching loads chunks before the user navigates to them using link rel="prefetch" headers. Prefetch-on-hover loads chunks when the user hovers over a navigation link for instant transitions. Route-level code splitting reduces initial bundle size significantly. Monitor your chunks with your build tool analyzer to identify opportunities. Group frequently visited routes together and separate rarely visited admin or settings routes into separate chunks.' },
      { id: 'layout-patterns', title: 'Layout Patterns', content: 'Vue Router enables flexible layout systems using nested routes and router-view. The top-level App component renders a router-view for the main layout. Each layout (default admin auth) is a component with its own router-view for page content. Child routes render inside their parent layout component. Named router-views support multiple outlets in a single layout like a sidebar and main content area. You can nest layouts deeply — a dashboard layout might have a sidebar layout which has a content layout. Dynamic layouts switch based on route meta or user role. A middleware pattern checks conditions before rendering a layout. The keep-alive pattern wraps router-view in keep-alive to preserve component state across navigation. For authentication use a guard on the layout component that redirects unauthenticated users. Layout transitions use the transition component around router-view for page transition animations.' }
    ],
    quiz: [
      { id: 1, question: 'How do you access a dynamic route parameter in the Composition API?', options: ['this.$route.params', 'useRoute().params', 'useRouter().params', 'defineProps().params'], correctIndex: 1, explanation: 'The useRoute() composable returns the current route object which includes the params property for dynamic segments.' },
      { id: 2, question: 'How do you implement route-level code splitting?', options: ['Using webpack config only', 'Using dynamic imports in the route component property', 'By splitting files manually', 'It is automatic'], correctIndex: 1, explanation: 'Dynamic imports like component: () => import("./MyPage.vue") create separate chunks that load only when the route is visited.' },
      { id: 3, question: 'How do you create nested layouts with Vue Router?', options: ['Using CSS grid only', 'By nesting routes with children and each parent rendering a router-view', 'Using layout components from Vue core', 'Layouts are not possible with Vue Router'], correctIndex: 1, explanation: 'Nested routes where each parent component contains a router-view allow building complex layout hierarchies with page content rendering inside layouts.' }
    ]
  },
  {
    id: 539, slug: 'vue-router-guards', title: 'Navigation Guards',
    description: 'Control route access and navigation flow with global and per-route guards.',
    level: 'intermediate', duration: '30 min',
    objectives: ['Implement global before-each and after-each guards', 'Use per-route before-enter guards and in-component guards', 'Build authentication and authorization flows with navigation guards'],
    topics: [
      { id: 'global-guards', title: 'Global Guards', content: 'Global navigation guards run on every route transition. router.beforeEach is the most commonly used guard and receives to (destination route) from (origin route) and a next function or returns a value. It is used for authentication checks redirecting unauthenticated users to login. router.afterEach runs after every navigation completes and is useful for analytics logging or scrolling behavior. router.beforeResolve runs after all in-component guards and async components are resolved but before navigation is confirmed. The beforeEach guard is the primary authentication gate — check if the route requires auth by reading route.meta.requiresAuth and verify the user is authenticated. If not redirect to the login page with a redirect query parameter so the user returns after login. Guards can be added globally in the router configuration or inside the setup function. Multiple global guards run in registration order.' },
      { id: 'per-route-guards', title: 'Per-Route and In-Component Guards', content: 'Per-route guards are defined on individual route objects using the beforeEnter property. This guard only runs when navigating to that specific route. It receives the same to from and next arguments as global guards. Per-route guards are useful for route-specific access control like admin pages or feature flags. In-component guards are defined inside the component using the Composition API. onBeforeRouteLeave runs before the user navigates away from the component. onBeforeRouteUpdate runs when the route changes but the component is reused (like changing a user ID parameter). These guards are essential for preventing accidental navigation when a form has unsaved changes. The leave guard can show a confirmation dialog and cancel navigation if the user chooses to stay. Component guards have access to the component instance context making them ideal for checking component-level state before allowing navigation.' },
      { id: 'auth-patterns', title: 'Authentication Patterns', content: 'Navigation guards are the foundation of authentication and authorization in Vue Router applications. The global beforeEach guard checks if the destination route has requiresAuth meta. If it does verify the authentication token or user state from your auth store. If the user is not authenticated redirect to /login with a query parameter containing the intended destination. The login page reads this parameter and redirects back after successful login. For role-based access check user roles against route.meta.requiredRoles. If the user lacks the required role redirect to an unauthorized page. Async guards handle token refresh by awaiting the refresh call before proceeding. The guard pattern also manages loading states — set a global loading flag while the guard runs. Combine guards with Pinia stores for clean separation between routing logic and authentication state. Test guards by mocking the router and verifying redirect behavior for different authentication states.' }
    ],
    quiz: [
      { id: 1, question: 'When does the router.beforeEach guard execute?', options: ['After navigation completes', 'Before every route transition', 'Only for authenticated routes', 'During page load only'], correctIndex: 1, explanation: 'router.beforeEach runs before every route transition making it the primary place for authentication and access control checks.' },
      { id: 2, question: 'What does onBeforeRouteLeave do?', options: ['It runs before the component mounts', 'It runs before the user navigates away from the current component', 'It runs after navigation completes', 'It prevents all navigation'], correctIndex: 1, explanation: 'onBeforeRouteLeave is an in-component guard that runs before the user navigates away, useful for preventing accidental navigation with unsaved changes.' },
      { id: 3, question: 'How do you redirect back to the original page after login?', options: ['Using localStorage only', 'By passing the intended destination as a query parameter in the login redirect', 'By using history.back()', 'By hardcoding the redirect URL'], correctIndex: 1, explanation: 'Store the intended destination in a query parameter when redirecting to login, then read it after successful login to navigate back.' }
    ]
  },
  {
    id: 540, slug: 'vue-dynamic-routing', title: 'Dynamic Routes',
    description: 'Create dynamic and programmatic routes that adapt to application state and data.',
    level: 'intermediate', duration: '30 min',
    objectives: ['Add and remove routes dynamically at runtime', 'Use dynamic route matching with advanced patterns', 'Build programmatic navigation with named routes and params'],
    topics: [
      { id: 'dynamic-matching', title: 'Dynamic Route Matching', content: 'Vue Router supports dynamic route segments using colon syntax in path definitions. A route like /user/:id matches /user/123 and captures 123 as params.id. Multiple dynamic segments work like /user/:userId/post/:postId. Optional parameters use the ? suffix like /user/:id?. Catch-all segments use the asterisk syntax like /path/:pathMatch(.*) to capture everything after a prefix. Custom regex patterns constrain parameter matching like /user/:id(\\d+) for numeric IDs only. Named parameters provide semantic names to dynamic segments. The matched route is accessible via useRoute() which returns the current route object including params path matched routes and query parameters. Route matching follows a priority order — static routes match before dynamic ones and more specific patterns before less specific ones. Repeatable parameters use the plus syntax like /file/:path+ to match multiple segments.' },
      { id: 'programmatic-navigation', title: 'Programmatic Navigation', content: 'Vue Router provides several methods for programmatic navigation. useRouter returns the router instance with push replace and go methods. router.push navigates to a new URL and adds a history entry. It accepts a path string like /user/123 or a route object with name and params. router.replace navigates without adding a history entry. router.go moves forward or backward in history like browser back and forward buttons. router.back and router.forward are convenience methods for single-step navigation. Named routes with params are the recommended way to navigate because they decouple navigation from URL structure. router.resolve creates a route object from a route location without navigating. For navigation with confirmation use the onBeforeRouteLeave guard or a custom confirmation dialog. Query parameters are passed as an object like { query: { sort: \'name\' } } and are accessible via useRoute().query.' },
      { id: 'dynamic-route-addition', title: 'Adding Routes at Runtime', content: 'Vue Router allows adding routes after the router is created using router.addRoute. This is useful for applications that load routes dynamically based on user permissions or plugin configuration. router.addRoute adds a single route object to the router. When adding a parent route with children the child routes are added under that parent. router.removeRoute removes a route by its name. This enables role-based routing where admin routes are only added for admin users. Dynamic route addition is also useful for micro-frontends where each sub-application registers its own routes. Plugin systems use addRoute to register routes from external modules. When adding routes dynamically ensure the route has a unique name to prevent conflicts. Navigation to newly added routes works immediately. However added routes are not persistent — they disappear when the page reloads so you need to re-add them in the router setup or a plugin.' }
    ],
    quiz: [
      { id: 1, question: 'How do you add a route dynamically after the router is created?', options: ['router.push()', 'router.addRoute()', 'router.define()', 'router.register()'], correctIndex: 1, explanation: 'router.addRoute() adds a new route to the router instance after creation, useful for permission-based routing.' },
      { id: 2, question: 'What does the plus syntax do in route parameters?', options: ['Makes the parameter required', 'Matches multiple path segments', 'Adds a default value', 'Enables regex mode'], correctIndex: 1, explanation: 'The plus syntax like :path+ matches one or more path segments, allowing capture of multiple segments in a single parameter.' },
      { id: 3, question: 'How do you navigate without adding a history entry?', options: ['router.push()', 'router.go()', 'router.replace()', 'router.back()'], correctIndex: 2, explanation: 'router.replace() navigates to a new URL but replaces the current history entry instead of adding a new one.' }
    ]
  },
  {
    id: 541, slug: 'vue-testing-vitest', title: 'Vitest Testing',
    description: 'Write unit and component tests for Vue applications using Vitest and Vue Test Utils.',
    level: 'intermediate', duration: '35 min',
    objectives: ['Set up Vitest for Vue project testing', 'Write unit tests for composables and reactive logic', 'Test Vue components with Vue Test Utils mounting and assertions'],
    topics: [
      { id: 'vitest-setup', title: 'Vitest Setup', content: 'Vitest is the recommended test runner for Vite-based Vue projects. It uses the same configuration as Vite so setup is minimal. Install vitest @vue/test-utils and jsdom for DOM simulation. Add a test script to package.json that runs vitest. Vitest provides globals like describe it expect and vi for mocking without imports when you enable globals in the config. The vitest.config.ts file can extend your vite config and add test-specific configuration like environment coverage settings and test file patterns. Vitest watches for file changes by default in development. For component testing set the environment to jsdom or happy-dom to simulate a browser DOM. Vitest uses the same transform pipeline as Vite so TypeScript JSX and Vue SFC files work out of the box. Coverage is built in using c8 or istanbul. Vitest runs tests in parallel by default for speed.' },
      { id: 'unit-testing', title: 'Unit Testing Composables', content: 'Unit testing composables involves calling the composable function and asserting on its return values. For simple composables that do not depend on Vue\'s reactivity system test them directly as plain functions. For composables using ref reactive or lifecycle hooks wrap them with the setup function from Vue Test Utils. Mount a test component with the composable in its setup and access the exposed values. Use flushPromises to wait for async operations. Test that reactive values update correctly when inputs change. For composables that use onMounted create a wrapper component and verify the mounted side effect. Mock external dependencies using vi.mock to isolate the composable under test. Test edge cases like empty inputs error states and boundary conditions. Organize tests in files adjacent to the source files following the name.spec.ts convention. Each test should be independent and not rely on state from other tests.' },
      { id: 'component-testing', title: 'Component Testing', content: 'Component testing with Vue Test Utils involves mounting the component and interacting with it through the wrapper API. mount creates a full component instance while shallowMount stubs child components for isolation. The wrapper provides methods to find elements via find and findAll trigger events via trigger read props via props and set props via setProps. For testing user interactions simulate clicks and input changes with trigger then assert on the updated DOM or emitted events. Test component rendering by asserting on the HTML output using wrapper.html. Test computed properties by setting props and verifying the rendered output. Test event emissions with wrapper.emitted and check the event payload. For testing components with Pinia create a testing pinia with createTestingPinia. For components using Vue Router wrap them in a test router. Mock API calls with vi.mock or msw for realistic integration tests.' }
    ],
    quiz: [
      { id: 1, question: 'Why is Vitest recommended for Vue projects using Vite?', options: ['It uses a different config system', 'It shares the same Vite configuration and transform pipeline', 'It is the only test runner that works with Vue', 'It does not need configuration'], correctIndex: 1, explanation: 'Vitest uses the same configuration and transform pipeline as Vite so TypeScript, JSX, and Vue SFC files work without extra setup.' },
      { id: 2, question: 'What is the difference between mount and shallowMount?', options: ['mount is faster', 'mount renders full component tree while shallowMount stubs child components', 'shallowMount renders all children', 'There is no difference'], correctIndex: 1, explanation: 'mount renders the full component tree including children while shallowMount stubs child components for faster isolated testing.' },
      { id: 3, question: 'How do you test emitted events from a component?', options: ['By checking the DOM text', 'Using wrapper.emitted() to access emitted event data', 'By reading the component state', 'By inspecting the router'], correctIndex: 1, explanation: 'wrapper.emitted() returns an object with event names as keys and arrays of payloads as values, allowing you to verify events were emitted correctly.' }
    ]
  },
  {
    id: 542, slug: 'vue-testing-library', title: 'Testing Library',
    description: 'Write user-centric component tests with Vue Testing Library focusing on behavior over implementation.',
    level: 'intermediate', duration: '30 min',
    objectives: ['Write tests that focus on user behavior rather than implementation details', 'Use Testing Library queries and user events for realistic interactions', 'Apply Testing Library principles for maintainable test suites'],
    topics: [
      { id: 'testing-library-philosophy', title: 'Testing Library Philosophy', content: 'Vue Testing Library is a set of utilities that encourage testing components the way users interact with them. The core principle is test what the user sees and does not how the component is implemented internally. Instead of testing component state or method calls test that the correct text appears and buttons respond to clicks. This approach makes tests resilient to implementation refactors — you can change internal state management from Options API to Composition API without breaking tests. The library provides query functions to find elements by role text label or placeholder. Prefer queries by role (getByRole getByLabelText) over selectors (getByTestId). The userEvent API simulates realistic user interactions including typing clicking and tabbing. Tests should resemble how a user would use the component — find a button by its label click it and verify the result appears. This philosophy produces tests that are readable maintainable and provide confidence in user-facing behavior.' },
      { id: 'queries-and-events', title: 'Queries and User Events', content: 'Testing Library provides query methods to find elements following a priority order. getByRole is the primary query — use it to find buttons inputs headings and other ARIA elements. getByLabelText finds form elements by their label. getByPlaceholderText finds inputs by placeholder. getByText finds elements by their text content. getAllBy variants return arrays when multiple elements match. queryBy returns null instead of throwing for elements that should not exist. findByRole is async and waits for elements to appear. User events are simulated using the userEvent library which provides realistic browser-like interactions. userEvent.click clicks an element. userEvent.type types text into an input. userEvent.clear clears an input. userEvent.tab moves focus. These events fire the correct DOM events including input focus and change. Always use userEvent instead of fireEvent for more realistic interactions. Assert results using expect with matchers like toBeInTheDocument toHaveTextContent and toHaveClass.' },
      { id: 'best-practices', title: 'Testing Library Best Practices', content: 'Follow these best practices for effective Testing Library tests. Use the most accessible query — prefer getByRole over getByTestId and add ARIA attributes to your components for better accessibility and testability. Avoid testing implementation details like component methods state variables or internal structure. Test user-visible behavior like text changes error messages and navigation. Use screen object for queries to simplify imports and improve readability. Clean up after tests using cleanup from @testing-library/vue to prevent state leaks. Mock only what you must — prefer real component behavior over mocks when feasible. Use msw for API mocking instead of mocking fetch or axios directly. Write tests that a new developer could read and understand as specification of behavior. Group related assertions with describe blocks but keep tests focused on one behavior. Use beforeEach for common setup but prefer isolated test data in each test for clarity.' }
    ],
    quiz: [
      { id: 1, question: 'What is the preferred query for finding buttons in Testing Library?', options: ['getByTestId', 'getBySelector', 'getByRole', 'getByClass'], correctIndex: 2, explanation: 'getByRole is the preferred query because it finds elements by their ARIA role making tests more accessible and resilient to CSS/implementation changes.' },
      { id: 2, question: 'Why should you prefer userEvent over fireEvent?', options: ['userEvent is faster', 'userEvent simulates more realistic browser interactions including focus and change events', 'fireEvent does not work with Vue', 'There is no difference'], correctIndex: 1, explanation: 'userEvent fires a complete sequence of DOM events (focus, input, change) mimicking real user behavior, while fireEvent fires only the single event.' },
      { id: 3, question: 'What should Testing Library tests focus on?', options: ['Component internal state and methods', 'User-visible behavior and output', 'CSS styling', 'Build configuration'], correctIndex: 1, explanation: 'Tests should focus on what users see and do — text content, button behavior, form submissions — not internal implementation details.' }
    ]
  },
  {
    id: 543, slug: 'vue-ssr-nuxt', title: 'SSR with Nuxt',
    description: 'Build server-rendered Vue applications with Nuxt 3 for improved performance and SEO.',
    level: 'advanced', duration: '45 min',
    objectives: ['Understand how SSR renders Vue on the server and hydrates on the client', 'Configure Nuxt 3 for server-side rendering and API routes', 'Handle data fetching server-side with useFetch and useAsyncData'],
    topics: [
      { id: 'ssr-concepts', title: 'SSR Concepts', content: 'Server-side rendering generates the initial HTML on the server and sends it to the browser. The browser displays the HTML immediately while Vue hydrates it adding interactivity. Without SSR (client-side rendering) the browser downloads an empty HTML shell and JavaScript renders everything which delays content visibility. SSR provides faster first contentful paint better SEO since search engines see rendered HTML and improved social media sharing with server-rendered meta tags. The server executes the Vue component tree produces HTML and serializes component state. The client downloads the page hydrates the HTML with event listeners and reactive behavior using the serialized state. Hydration matches the server-rendered DOM with the client-side component tree. Nuxt 3 automates this entire process with zero configuration. You write Vue components and Nuxt handles server rendering client hydration and build optimization.' },
      { id: 'nuxt-fetching', title: 'Nuxt Data Fetching', content: 'Nuxt 3 provides useFetch and useAsyncData composables for data fetching that works on both server and client. useFetch is a simplified wrapper around useAsyncData and $fetch that handles loading states errors and caching. It deduplicates requests between server and client to avoid fetching the same data twice. useAsyncData gives more control over caching key generation and transformation. Both composables run on the server during SSR and on the client during navigation. The key parameter ensures the same request is not repeated. useLazyFetch and useLazyAsyncData do not block navigation and show loading states instead. For API routes create files in the server/api directory which become server endpoints. The server directory also supports middleware plugins and utils. Nitro the server engine in Nuxt 3 handles deployment to multiple platforms including Node.js Cloudflare Workers and serverless functions.' },
      { id: 'nuxt-plugins', title: 'Nuxt Plugins and Middleware', content: 'Nuxt 3 plugins run during application initialization and are useful for injecting global services or setting up authentication. Plugins in the plugins directory execute on every page load both server-side and client-side. Server-only plugins run only on the server using .server suffix. Client-only plugins use .client suffix. Nuxt middleware runs before each route navigation. Global middleware in the middleware directory runs on every route. Named middleware is attached to specific routes. Route middleware handles authentication authorization and redirects. The defineNuxtPlugin function provides a typed way to create plugins. Nuxt plugins receive the nuxtApp object which gives access to the Vue app instance Pinia stores and the router. For authentication use a plugin to restore auth state from cookies and a middleware to protect routes. Nuxt auto-imports composables from the composables directory and utilities from the utils directory.' }
    ],
    quiz: [
      { id: 1, question: 'What is the main benefit of SSR for SEO?', options: ['Faster JavaScript execution', 'Search engines can crawl fully rendered HTML content', 'Smaller bundle size', 'Better developer experience'], correctIndex: 1, explanation: 'SSR sends fully rendered HTML to the browser so search engines can crawl and index the content without executing JavaScript.' },
      { id: 2, question: 'What does useFetch do differently from a regular fetch call?', options: ['It uses XMLHttpRequest instead', 'It deduplicates requests between server and client and handles loading/error states', 'It caches responses indefinitely', 'It only works on the server'], correctIndex: 1, explanation: 'useFetch deduplicates server and client requests to avoid fetching twice, and provides reactive loading and error states automatically.' },
      { id: 3, question: 'How do you create a Nuxt server-only plugin?', options: ['Place it in plugins/ directory', 'Add .server suffix to the plugin filename', 'Use defineServerPlugin()', 'Set ssr: true in nuxt.config'], correctIndex: 1, explanation: 'Adding .server suffix to a plugin filename makes it run only on the server, preventing client-side execution.' }
    ]
  },
  {
    id: 544, slug: 'vue-ssg', title: 'Static Site Generation',
    description: 'Generate static HTML files at build time with Nuxt for blazing-fast content sites.',
    level: 'advanced', duration: '35 min',
    objectives: ['Configure Nuxt for static site generation with the generate command', 'Understand when SSG is preferable to SSR or CSR', 'Handle dynamic routes and API data in static generation'],
    topics: [
      { id: 'ssg-overview', title: 'SSG Overview', content: 'Static site generation pre-renders every page into HTML files at build time. These files are served directly from a CDN without any server processing. SSG combines the SEO benefits of SSR with the simplicity and speed of static hosting. Each page is generated once during build and cached forever until the next build. This makes SSG ideal for content that does not change frequently like documentation marketing pages and blogs. Nuxt 3 supports SSG through the nuxi generate command which uses Nitro to pre-render all routes. The output is a directory of HTML files CSS assets and JavaScript bundles. You can deploy SSG output to any static hosting provider like Netlify Vercel GitHub Pages or Cloudflare Pages. The tradeoff is that content updates require a rebuild and redeploy. For content that changes often use SSR or client-side rendering with API calls.' },
      { id: 'dynamic-routes', title: 'Dynamic Routes in SSG', content: 'Static generation needs to know all possible routes at build time to generate HTML for each one. For dynamic routes like /blog/:slug you must provide a list of all slugs. In Nuxt 3 use the routeRules configuration or a crawler function to discover dynamic routes. The generate.routes option in nuxt.config specifies an array of route strings or route objects. For data-driven pages fetch all possible entries at build time and return their paths. The crawler function automatically discovers links in your pages and generates them. The payload extraction feature in Nuxt 3 fetches data during generation and embeds it in the HTML so the client does not need to re-fetch. Fallback routes use the setting to either show a 404 or use client-side rendering for unknown routes. Incremental static regeneration regenerates specific pages on a schedule without rebuilding the entire site.' },
      { id: 'ssg-deployment', title: 'SSG Deployment', content: 'Deploying an SSG Nuxt application involves generating the static files and uploading them to a hosting provider. Run nuxt generate to create the output directory. The output contains HTML files a _payload.js for each page and static assets. Deploy to Netlify by connecting your repository and setting the build command to nuxt generate and publish directory to .output/public. Vercel auto-detects Nuxt projects. For Cloudflare Pages set the build command similarly. GitHub Pages requires the output to be in a docs directory or gh-pages branch. Configure your hosting provider with proper headers for caching HTML files with short TTL and assets with long TTL. Set up redirects for clean URLs. For forms use a third-party form service since static sites cannot handle server-side form submissions. Add analytics tracking and error monitoring to your generated pages. The pre-rendered pages load instantly from CDN edge locations worldwide.' }
    ],
    quiz: [
      { id: 1, question: 'When is SSG preferable to SSR?', options: ['When content changes every second', 'When content is relatively static and does not require server processing', 'When you need real-time user data', 'When you have thousands of dynamic routes'], correctIndex: 1, explanation: 'SSG pre-renders pages at build time making it ideal for content that does not change frequently like documentation and marketing pages.' },
      { id: 2, question: 'How does Nuxt generate pages for dynamic routes in SSG?', options: ['It guesses the routes', 'You provide a list of routes or use a crawler to discover them at build time', 'Dynamic routes are not supported in SSG', 'It uses server-side rendering as fallback'], correctIndex: 1, explanation: 'You must provide all possible dynamic routes at build time either through a routes array or a crawler function that discovers links.' },
      { id: 3, question: 'What is the main tradeoff of SSG compared to SSR?', options: ['Worse SEO', 'Content updates require a rebuild and redeploy', 'Slower page loads', 'No JavaScript support'], correctIndex: 1, explanation: 'SSG generates static files at build time so any content update requires rebuilding the site, unlike SSR which generates fresh content on each request.' }
    ]
  },
  {
    id: 545, slug: 'vue-auto-imports', title: 'Auto Imports',
    description: 'Automatically import Vue APIs and composables to reduce boilerplate with unplugin-auto-import.',
    level: 'intermediate', duration: '25 min',
    objectives: ['Configure unplugin-auto-import for Vue and composables', 'Understand what gets auto-imported and how to customize imports', 'Use auto-imports safely with TypeScript and IDE support'],
    topics: [
      { id: 'auto-import-setup', title: 'Auto Import Setup', content: 'Unplugin-auto-import automatically imports commonly used Vue APIs like ref reactive computed watch and lifecycle hooks so you do not need explicit import statements in every file. Install the package and add it to your Vite config plugins array. Configure the imports option to specify which APIs to auto-import. The vue preset imports all Vue Composition API functions. You can add presets for Vue Router VueUse and Pinia. The dts option generates TypeScript declaration files so your IDE provides autocomplete and type checking. Auto-imports reduce boilerplate in every component file. Instead of importing ref from vue at the top of every file you just use ref directly. The plugin transforms your code at build time adding the necessary import statements. Nuxt 3 has auto-imports built in for Vue and Nuxt composables. For non-Nuxt projects unplugin-auto-import provides the same functionality.' },
      { id: 'auto-import-custom', title: 'Custom Auto Imports', content: 'You can auto-import your own composables and utilities by configuring directories. The dirs option specifies directories to scan for exports. For example setting dirs to [\'./composables\'] auto-imports all exported functions from that directory. This is especially useful in large projects with many shared composables. You can also specify individual imports as an array of [importName source] tuples. The auto-import plugin generates a virtual module that re-exports the detected APIs. The generated declaration file (auto-imports.d.ts) provides TypeScript types so the auto-imported names are properly typed. Custom presets group related imports for reuse across projects. When using auto-imports with barrels (index.ts files that re-export) be careful about tree-shaking — barrel imports can pull in entire modules. The plugin handles this by importing directly from the source files. IDE integration works through the generated type declarations which provide autocomplete suggestions.' },
      { id: 'auto-import-safety', title: 'Auto Import Safety', content: 'Auto-imports improve developer experience but require awareness of potential issues. Name collisions can occur if a local variable has the same name as an auto-imported API. The plugin resolves local names first so explicit imports or local definitions take precedence. Avoid using auto-imports for names that commonly conflict like map or filter. Documentation is important — new team members may not realize certain APIs are available without imports. Include auto-imported APIs in your code style guide and add ESLint rules to catch undefined references. The generated type declarations ensure TypeScript catches errors. Performance is not impacted since auto-imports are resolved at build time not runtime. For tree-shaking auto-imports work like regular imports — unused APIs are removed. Review the generated import statements periodically to ensure they match expectations. Consider adding a comment convention in your team to distinguish auto-imported from manually imported APIs.' }
    ],
    quiz: [
      { id: 1, question: 'What does unplugin-auto-import do?', options: ['Runs tests automatically', 'Automatically imports Vue APIs and composables without explicit import statements', 'Generates component code', 'Optimizes bundle size'], correctIndex: 1, explanation: 'unplugin-auto-import scans your code and adds import statements for configured APIs at build time, eliminating manual imports.' },
      { id: 2, question: 'How do you auto-import your own composables?', options: ['By importing them manually', 'By configuring the dirs option to specify directories to scan', 'By adding them to package.json', 'Auto-imports only work for Vue core APIs'], correctIndex: 1, explanation: 'The dirs option in the auto-import config specifies directories containing your composables which the plugin scans and auto-imports.' },
      { id: 3, question: 'What happens if a local variable name conflicts with an auto-imported API?', options: ['The auto-import overwrites the local variable', 'The local variable takes precedence over the auto-import', 'Both are available', 'The build fails'], correctIndex: 1, explanation: 'Local variables take precedence over auto-imports preventing naming conflicts from causing unexpected behavior.' }
    ]
  },
  {
    id: 546, slug: 'vue-virtual-scroll', title: 'Virtual Scrolling',
    description: 'Efficiently render large lists with virtual scrolling to only render visible items.',
    level: 'advanced', duration: '35 min',
    objectives: ['Implement virtual scrolling for large datasets in Vue', 'Use vue-virtual-scroller and other virtual scroll libraries', 'Handle dynamic item sizes and scroll position restoration'],
    topics: [
      { id: 'virtual-scroll-concepts', title: 'Virtual Scroll Concepts', content: 'Virtual scrolling renders only the visible items in a list instead of all items. When you have thousands of items rendering every DOM node causes performance issues with slow initial renders and high memory usage. Virtual scrolling calculates which items are visible based on the scroll position and container height and renders only those items plus a small buffer. The container has a scroll height equal to the total list height but only contains DOM nodes for visible items. As the user scrolls new items are rendered and old ones are removed. This keeps the DOM node count constant regardless of list size. The main challenges are calculating the total scroll height estimating item heights for positioning and efficiently recycling DOM nodes as the user scrolls. Libraries like vue-virtual-scroller and vue-virtual-scroll-grid handle these complexities.' },
      { id: 'vue-virtual-scroller', title: 'Vue Virtual Scroller', content: 'Vue Virtual Scroller is a popular library providing ready-made virtual scroll components. RecycleScroller renders only visible items in a dynamic list with fixed or variable item sizes. DynamicScroller handles items with unpredictable heights by measuring them after rendering. DynamicScrollerItem wraps each item and handles positioning within the dynamic scroller. Install with npm install vue-virtual-scroller and import the CSS file for default styles. The component requires a unique key for each item the total item count and a slot component for rendering items. The slot receives the item data and index. For variable heights set the size-item prop to a function that returns the estimated height. The buffer option adds extra items above and below the visible area for smoother scrolling. Handle scroll events using @scroll for loading indicators or scroll position tracking. The library handles resize events and scroll position restoration automatically.' },
      { id: 'virtual-scroll-patterns', title: 'Virtual Scroll Patterns', content: 'Several patterns enhance virtual scrolling implementations. Infinite scroll loads more data as the user scrolls near the bottom combining virtual scrolling with pagination. Detect the scroll position using onScroll and trigger a data fetch when the user reaches a threshold. Scroll position restoration saves and restores the scroll position when navigating back to a list. Use sessionStorage or the router state to persist the position. Sticky headers within virtual lists require special handling since the virtual scroller positions items absolutely. The sticky header technique calculates when a header should become fixed at the top as the user scrolls. Grouped lists organize items into sections with headers. The virtual scroller must account for group headers in its height calculations. Search filtering changes the list dynamically — maintain the scroll position by calculating the offset of the first matching item. Performance tuning involves adjusting buffer sizes and item height estimates to balance memory usage against scroll smoothness.' }
    ],
    quiz: [
      { id: 1, question: 'What is the main performance benefit of virtual scrolling?', options: ['It compresses data', 'It renders only visible items keeping DOM node count constant', 'It loads items faster from the server', 'It uses less CSS'], correctIndex: 1, explanation: 'Virtual scrolling renders only the items visible in the viewport plus a buffer, keeping the DOM node count constant regardless of total list size.' },
      { id: 2, question: 'How does Vue Virtual Scroller handle variable item heights?', options: ['It assumes all items are the same height', 'DynamicScroller measures items after rendering and positions them accordingly', 'It uses CSS grid for sizing', 'Variable heights are not supported'], correctIndex: 1, explanation: 'DynamicScroller measures each item after rendering and uses the actual height for positioning calculations enabling variable-size items.' },
      { id: 3, question: 'What challenge do sticky headers present in virtual lists?', options: ['They cause memory leaks', 'They require special positioning since virtual scrollers use absolute positioning', 'They prevent scrolling', 'They only work with fixed heights'], correctIndex: 1, explanation: 'Virtual scrollers position items absolutely so sticky headers need special handling to detect when they should become fixed at the viewport top.' }
    ]
  },
  {
    id: 547, slug: 'vue-web-components', title: 'Web Components',
    description: 'Package Vue components as standard Web Components for framework-agnostic use.',
    level: 'advanced', duration: '35 min',
    objectives: ['Build Web Components from Vue components using Vue CE', 'Handle props events and slots in the Web Component boundary', 'Use Web Components across different frameworks and vanilla JavaScript'],
    topics: [
      { id: 'web-component-basics', title: 'Web Component Basics', content: 'Web Components are a set of browser standards that let you create reusable custom elements encapsulated with Shadow DOM. Vue provides the defineCustomElement function which wraps a Vue component as a Web Component. The resulting custom element can be used in any HTML page regardless of the framework. Define a Vue component as usual then pass it to defineCustomElement which returns a custom element class. Register it with customElements.define then use it as a regular HTML tag. The Shadow DOM provides style encapsulation — styles inside the component do not leak out and external styles do not leak in. Web Components receive props as HTML attributes and emit events as CustomEvents. Slots work through the standard slot element providing content projection. Web Components are useful for design systems shared across multiple teams using different frameworks.' },
      { id: 'vue-ce-setup', title: 'Vue Custom Element Setup', content: 'Setting up a Vue component as a custom element involves a few specific considerations. Import defineCustomElement from vue and wrap your component. The component must define its props using defineProps since they become HTML attributes. Styles are handled differently — instead of scoped styles you need to provide styles as a string or import them with the help of a Vite plugin like vite-plugin-css-injected-by-js. Events are emitted using defineEmits and dispatch as CustomEvents on the root element. Slots work as expected but the consumer uses them as regular HTML slot syntax. The custom element hydrates when it is inserted into the DOM. Props are serialized as strings in HTML attributes so complex objects need JSON serialization. Boolean attributes follow HTML conventions where presence means true. For reactive updates the custom element watches attribute changes and updates the Vue component internally. Build the custom element as a library entry point for distribution.' },
      { id: 'web-component-patterns', title: 'Web Component Patterns', content: 'Web Components work well for specific architectural patterns. Design systems that serve multiple teams use Web Components to provide a framework-agnostic component library. Each team can integrate the components into their Angular React or Vue application. Micro-frontends use Web Components as the integration boundary between independently deployed applications. Third-party widgets like chat bubbles or payment forms use Web Components to embed into any website without framework conflicts. The limitations of Web Components include no SSR support for the Shadow DOM, limited slot patterns compared to Vue slots, and attribute serialization for props. For complex data use JSON attributes or set properties directly on the element. Style customization from outside requires CSS custom properties since Shadow DOM blocks regular CSS. Testing Web Components uses standard DOM APIs and tools like Testing Library. Ship Web Components as a single JavaScript bundle with all dependencies included for easy integration.' }
    ],
    quiz: [
      { id: 1, question: 'What function wraps a Vue component as a Web Component?', options: ['createApp', 'defineCustomElement', 'defineComponent', 'createElement'], correctIndex: 1, explanation: 'defineCustomElement from vue wraps a Vue component as a standard custom element that can be used in any HTML page.' },
      { id: 2, question: 'What does Shadow DOM provide for Web Components?', options: ['Server-side rendering', 'Style encapsulation preventing styles from leaking in or out', 'Faster rendering', 'Better SEO'], correctIndex: 1, explanation: 'Shadow DOM encapsulates styles and DOM structure so component styles do not conflict with the host page styles.' },
      { id: 3, question: 'How do Web Components communicate with their parent page?', options: ['Through Vue-specific events', 'Through CustomEvent API and HTML attributes', 'Through global variables', 'Through localStorage'], correctIndex: 1, explanation: 'Web Components use standard CustomEvent for output and HTML attributes for input, making them framework-agnostic.' }
    ]
  },
  {
    id: 548, slug: 'vue-i18n', title: 'Internationalization',
    description: 'Add multi-language support to Vue applications using Vue I18n.',
    level: 'intermediate', duration: '35 min',
    objectives: ['Set up Vue I18n for multi-language support', 'Use message translation with interpolation and plurals', 'Implement locale switching and lazy-loaded translations'],
    topics: [
      { id: 'i18n-setup', title: 'I18n Setup', content: 'Vue I18n is the official internationalization library for Vue 3. Install it with npm install vue-i18n and create a locale configuration. Create translation message files for each language like en.json and fr.json containing key-value pairs. Initialize the I18n instance with the default locale and message files. Install it as a Vue plugin using app.use(i18n). In templates use the $t function or the useI18n composable to access translations. The t function takes a message key and returns the translated string. Compose messages using interpolation with named placeholders like {name}. Nested message keys use dot notation like auth.login.title. Set the locale to switch languages dynamically. The locale property is reactive so changing it updates all translations in the template. For TypeScript create typed message schemas using the vue-i18n type generation tool.' },
      { id: 'i18n-features', title: 'I18n Features', content: 'Vue I18n supports rich message formatting beyond simple text replacement. Named interpolation lets you embed variables with placeholders like Hello {name}. List interpolation uses numbered slots for ordered parameters. Pluralization handles singular and plural forms based on a count parameter using pipe syntax like no items | one item | {count} items. Date and number formatting use the linked @: syntax to reference locale-specific formatters. HTML in messages supports rendering rich text by marking messages as safe HTML. Fall-back languages chain multiple locales so English is used when a translation is missing in the current locale. Lazy loading splits translation files by locale and loads them on demand reducing the initial bundle. Composition API support via useI18n provides reactive locale and message access. The reactivity system updates all translations when the locale changes without page reload.' },
      { id: 'i18n-patterns', title: 'I18n Patterns', content: 'Effective internationalization follows several patterns. Organize translation keys by feature or component rather than by language. Use a flat key structure like auth.login.button instead of deeply nested objects for better developer experience. Generate translation keys from component names for consistency. Extract all user-visible strings into translation files — do not hardcode text in components. Handle pluralization for counts dates and quantities. Format numbers currencies and dates using the locale-aware formatting APIs. Consider text length variations — German translations are often 30 percent longer than English which can break layouts. Test translations by switching the locale during development. For SEO include the correct hreflang tags and localized URLs. Lazy load translation files for locales the user does not need initially. Use a translation management platform like Crowdin or Lokalise for collaborative translation workflows.' }
    ],
    quiz: [
      { id: 1, question: 'How do you access a translation in a Vue template?', options: ['translate()', '$t() or useI18n()', 'i18n.t()', 'msg()'], correctIndex: 1, explanation: 'Use $t() in templates or the useI18n composable in the Composition API to access translated messages.' },
      { id: 2, question: 'How does Vue I18n handle pluralization?', options: ['With if-else statements', 'With pipe syntax in messages like singular | plural | count form', 'With separate message keys', 'Pluralization is not supported'], correctIndex: 1, explanation: 'Pluralization uses pipe-separated forms in a single message key where the index is determined by the count parameter.' },
      { id: 3, question: 'What is lazy loading in the context of I18n?', options: ['Loading translations slowly', 'Loading translation files on demand when the user switches locale', 'Loading all translations at startup', 'Delaying component rendering'], correctIndex: 1, explanation: 'Lazy loading splits translation files by locale and loads them only when needed reducing the initial bundle size.' }
    ]
  },
  {
    id: 549, slug: 'vue-performance', title: 'Performance Optimization',
    description: 'Optimize Vue application performance with rendering strategies and profiling techniques.',
    level: 'advanced', duration: '40 min',
    objectives: ['Identify and fix common Vue performance bottlenecks', 'Apply virtual scrolling and lazy loading for large datasets', 'Use Vue Devtools profiler to measure and improve component render times'],
    topics: [
      { id: 'rendering-performance', title: 'Rendering Performance', content: 'Vue\'s reactivity system automatically tracks dependencies and re-renders only affected components. However unnecessary re-renders still occur when parent components re-render children that do not need updates. Use the v-once directive for static content that never changes — it caches the initial render and skips re-rendering. Use v-memo for conditional memoization of specific template sections. The shallowRef and shallowReactive options reduce reactivity tracking overhead for large objects where deep reactivity is unnecessary. The markRaw function explicitly marks objects as non-reactive preventing Vue from making them reactive. For large lists use virtual scrolling to render only visible items. Component-level optimization with Vue.memo equivalent (no direct equivalent but achieved through component structure) prevents unnecessary child re-renders. Analyze your bundle size to identify large dependencies that can be lazy loaded or replaced with lighter alternatives.' },
      { id: 'code-splitting', title: 'Code Splitting', content: 'Code splitting divides your application into smaller chunks that load on demand. Vue Router supports route-level code splitting using dynamic imports in the component property. Each route becomes a separate chunk that loads only when visited. Component-level splitting uses defineAsyncComponent for components that are not immediately needed. The import.meta.glob helper in Vite creates dynamic imports for all files matching a pattern. Prefetching loads route chunks in the background during idle time using link rel="prefetch" headers. Preloading loads critical chunks immediately using link rel="preload". Analyze your bundle with vite-plugin-visualizer to identify large dependencies. Split vendor libraries into separate chunks so browser caching is more effective. Dynamic imports for feature flags allow conditional code loading. The tree-shaking built into Vite eliminates unused exports reducing bundle size. Monitor bundle size changes in CI to catch regressions.' },
      { id: 'profiling-tools', title: 'Profiling and Measurement', content: 'Measuring performance requires the right tools. Vue Devtools provides a component profiler showing render times and component counts. The performance tab records component updates and shows which components re-rendered during an interaction. The timeline view shows component lifecycle events over time. Browser DevTools Performance tab records JavaScript execution and shows which functions take the most time. Lighthouse audits for performance accessibility and best practices. The vite-bundle-analyzer visualizes your bundle composition to find large dependencies. Console.time and console.timeEnd measure specific operations. The performance.mark and performance.measure APIs provide precise timing measurements. For React-style profiling use the onRenderTracked and onRenderTriggered lifecycle hooks to debug which reactive dependencies cause re-renders. Network throttling in DevTools simulates slow connections for testing loading performance. Always measure on representative devices and network conditions.' }
    ],
    quiz: [
      { id: 1, question: 'What does the v-once directive do for performance?', options: ['Disables the component', 'Caches the initial render and skips subsequent re-renders', 'Makes the component reactive', 'Increases render frequency'], correctIndex: 1, explanation: 'v-once caches the initial render of an element or component and never re-renders it, improving performance for static content.' },
      { id: 2, question: 'How does route-level code splitting improve performance?', options: ['It makes routes faster', 'It loads route code only when the route is visited reducing initial bundle size', 'It removes unused routes', 'It caches route data'], correctIndex: 1, explanation: 'Code splitting loads route-specific JavaScript only when needed, reducing the initial bundle size and improving first load time.' },
      { id: 3, question: 'What tool shows which Vue components re-rendered during an interaction?', options: ['Webpack analyzer', 'Vue Devtools component profiler', 'ESLint', 'TypeScript compiler'], correctIndex: 1, explanation: 'Vue Devtools includes a component profiler that records and displays which components re-rendered during user interactions.' }
    ]
  },
  {
    id: 550, slug: 'vue-devtools', title: 'Vue DevTools',
    description: 'Debug and profile Vue applications effectively with Vue DevTools browser extension.',
    level: 'beginner', duration: '25 min',
    objectives: ['Inspect component trees and their state with Vue Devtools', 'Use the timeline and profiler to debug performance issues', 'Debug Pinia stores and Vue Router navigation'],
    topics: [
      { id: 'devtools-overview', title: 'DevTools Overview', content: 'Vue DevTools is a browser extension available for Chrome and Firefox that provides debugging capabilities specific to Vue applications. It adds a Vue panel to the browser DevTools with several tabs. The Components tab shows the component tree where you can select any component and inspect its props state computed properties and events. The Pinia tab displays all Pinia stores with their state and allows live editing. The Router tab shows route information including current route matched routes and navigation history. The Timeline tab records component lifecycle events and custom events over time. The inspector allows searching components by name. The component detail view shows reactive data refs and provides buttons to emit events. You can edit state values directly in the panel to test different states without changing code.' },
      { id: 'devtools-features', title: 'DevTools Features', content: 'Vue DevTools provides several powerful debugging features. Component inspection lets you click any element in the page and see which Vue component renders it. The component tree is navigable and highlights components on hover. State editing allows changing reactive values in real-time to test different scenarios without code changes. The event tracking tab shows events emitted by components with their payloads. Time travel debugging lets you step through component state changes chronologically. The snapshot feature captures the current state of all components for comparison. Custom events can be recorded using app.config.globalProperties.$devtools.emit. Performance profiling records render times for components and shows which ones re-render frequently. The Pinia tab provides a visual editor for store state with change tracking. Route inspection shows the current route parameters query and matched components. DevTools connects to the app via a WebSocket when the app runs in development mode.' },
      { id: 'devtools-tips', title: 'DevTools Tips', content: 'Effective use of Vue DevTools requires knowing specific techniques. Use the component filter to search by component name when working with large trees. The hierarchy view helps understand component nesting and identify unnecessary wrapper components. When debugging re-renders use the performance profiler to record an interaction then analyze which components re-rendered and why. The state diff feature shows exactly which properties changed between renders. For Pinia debugging track state changes over time and identify which actions modify which properties. Use the timeline to correlate user interactions with state changes. The component clipboard copies the component reference for debugging in the console. Enable the debug mode in Vue DevTools settings for more verbose logging. Pin frequently used components for quick access. In production mode Vue DevTools is disabled by default — enable it for testing with app.config.devtools = true. Combine Vue DevTools with browser DevTools Network tab for debugging API calls alongside component state.' }
    ],
    quiz: [
      { id: 1, question: 'Which Vue DevTools tab shows component state and props?', options: ['Router tab', 'Components tab', 'Timeline tab', 'Pinia tab'], correctIndex: 1, explanation: 'The Components tab displays the component tree where you can select any component and inspect its state props and computed values.' },
      { id: 2, question: 'What does time travel debugging allow you to do?', options: ['Travel to previous pages', 'Step through component state changes chronologically', 'Rewind browser history', 'Undo code changes'], correctIndex: 1, explanation: 'Time travel debugging lets you step through recorded state changes to see how component state evolved over time.' },
      { id: 3, question: 'Is Vue DevTools available in production builds?', options: ['Yes by default', 'No by default but can be enabled', 'Never available', 'Only in paid versions'], correctIndex: 1, explanation: 'Vue DevTools is disabled by default in production builds for security. It can be enabled with app.config.devtools for testing purposes.' }
    ]
  },
  {
    id: 551, slug: 'vue-typescript', title: 'TypeScript with Vue',
    description: 'Leverage TypeScript for type safety in Vue components with Composition API and SFC.',
    level: 'intermediate', duration: '35 min',
    objectives: ['Type component props and emits with defineProps and defineEmits', 'Create typed composables and reactive state with TypeScript', 'Configure TypeScript for optimal Vue development'],
    topics: [
      { id: 'typed-props', title: 'Typed Props', content: 'Vue 3 provides strong TypeScript integration through defineProps and defineEmits macros. In script setup define props using TypeScript generics like defineProps<{ title: string count: number }>(). This gives you full type checking and autocomplete for prop values in templates. For runtime validation combine type annotations with the withDefaults helper to specify default values. withDefaults takes the type definition and an object of defaults returning a fully typed props object. Props with complex types like arrays and objects work with TypeScript interfaces. Optional props use the ? syntax or are provided in withDefaults. Event typing uses defineEmits with a typed object like defineEmits<{ (e: string value: number): void }>(). This ensures event names and payloads are type-checked. The ComponentPropsOptions type allows extracting props types from existing components. Script setup macros are compile-time only — they do not exist at runtime.' },
      { id: 'typed-composables', title: 'Typed Composables', content: 'TypeScript composables benefit from explicit return types and generic parameters. A composable function should have typed parameters for its inputs and a typed return value. Use generics to make composables work with different types like a useCounter composable that accepts an initial value type. Ref and reactive values are inferred from their initial values so provide explicit types when the initial value is null. Use Ref<number | null> for nullable refs. Computed values are inferred from the getter function return type. When accepting callback parameters in composables type the function signature including parameters and return type. Vue provides utility types like ExtractPropTypes to extract component prop types. The MaybeRef type handles values that may be refs or plain values. For Pinia stores the store type is inferred from the setup function. Export composable interfaces and types alongside the function for consumers.' },
      { id: 'typescript-config', title: 'TypeScript Configuration', content: 'Configuring TypeScript for Vue requires specific tsconfig options. The compilerOptions.jsx option should be set to preserve for Vue\'s template compilation. The types array should include vue for Vue-specific types. The strict option should be enabled for maximum type safety. Vue\'s type definitions include global types like ComponentCustomProperties for adding properties to component instances and ComponentCustomProps for augmenting props types. The Volar extension for VS Code provides the best TypeScript support for Vue including template type checking. Volar replaces the old Vetur extension and understands both script and template sections. For auto-imported APIs the auto-import plugin generates type declarations. The defineProps defineEmits and defineExpose macros are typed at compile time. For library authors define component prop types using PropType or the generic syntax. The InstanceType utility type extracts the component instance type for template refs.' }
    ],
    quiz: [
      { id: 1, question: 'How do you type props in script setup with TypeScript?', options: ['Using PropTypes library', 'Using defineProps with TypeScript generics', 'Using @Prop decorator', 'Props cannot be typed'], correctIndex: 1, explanation: 'defineProps<{ title: string }>() provides type checking and autocomplete for props directly in script setup.' },
      { id: 2, question: 'What is the purpose of withDefaults?', options: ['To make all props required', 'To specify default values for typed props', 'To validate props at runtime', 'To convert props to refs'], correctIndex: 1, explanation: 'withDefaults combines TypeScript type annotations with runtime default values for props defined with defineProps.' },
      { id: 3, question: 'Which VS Code extension provides the best TypeScript support for Vue?', options: ['Vetur', 'Volar', 'ESLint', 'Prettier'], correctIndex: 1, explanation: 'Volar provides comprehensive TypeScript support for Vue including template type checking, replacing the older Vetur extension.' }
    ]
  },
  {
    id: 552, slug: 'vue-decorators', title: 'Class Component Decorators',
    description: 'Explore class-based Vue component syntax with decorators for TypeScript-heavy codebases.',
    level: 'advanced', duration: '30 min',
    objectives: ['Create class-based Vue components using vue-facing-decorator', 'Use decorators like Prop Watch Emit and Model', 'Understand the tradeoffs between class components and Composition API'],
    topics: [
      { id: 'class-component-basics', title: 'Class Component Basics', content: 'Class components provide an alternative way to define Vue components using TypeScript class syntax. The vue-facing-decorator library (successor to vue-property-decorator for Vue 3) lets you define components as classes with decorators. You extend the Vue class and use @Component decorator to mark the class as a Vue component. Class properties become reactive data. Methods are component methods. Getters become computed properties. Lifecycle hooks are class methods with their respective names like mounted and created. This syntax appeals to developers with backgrounds in Java C# or Angular where class-based patterns are common. Class components integrate well with TypeScript providing strong typing for all component members. However the Vue team recommends the Composition API with script setup as the primary approach. Class components are a community-maintained pattern and not part of Vue core.' },
      { id: 'decorators', title: 'Decorator Patterns', content: 'Class component decorators provide shorthand for common Vue features. The @Prop decorator defines component props with type information like @Prop({ required: true }) title string. The @Watch decorator creates watchers like @Watch("count") onCountChanged(newVal oldVal). The @Emit decorator marks methods as event emitters where the return value becomes the event payload. The @Model decorator creates v-model bindings. The @Ref decorator creates template references. The @Provide and @Inject decorators implement dependency injection. Each decorator adds metadata to the class that vue-facing-decorator reads during component creation. Decorators can accept options objects for configuration. For example @Watch can be configured with immediate and deep options. Multiple decorators of the same type can be stacked on a single property. The decorator pattern reduces boilerplate compared to the Options API while maintaining a class-based mental model.' },
      { id: 'class-vs-composition', title: 'Class vs Composition API', content: 'Class components and the Composition API serve the same purpose but with different mental models. Classes group related logic by type (data together methods together computed together) similar to the Options API. The Composition API groups logic by feature (all counter logic together all form logic together). Class components benefit from TypeScript class features like inheritance and access modifiers. The Composition API benefits from plain JavaScript familiarity and better tree-shaking. Class components have implicit reactivity through class property decorators. The Composition API has explicit reactivity through ref and reactive calls. For new projects the Composition API with script setup is recommended because it is the official Vue direction. Class components work well for teams migrating from Angular or Java who prefer class-based patterns. The vue-facing-decorator library provides the Vue 3 compatible implementation. Consider the long-term maintenance implications when choosing between approaches.' }
    ],
    quiz: [
      { id: 1, question: 'What does the @Prop decorator do in class components?', options: ['Creates a data property', 'Defines a component prop with type information', 'Watches a property for changes', 'Emits an event'], correctIndex: 1, explanation: 'The @Prop decorator marks a class property as a component prop with optional type validation and configuration.' },
      { id: 2, question: 'What is the Vue team\'s recommended approach for new components?', options: ['Class components with decorators', 'Options API', 'Composition API with script setup', 'JSX only'], correctIndex: 2, explanation: 'The Vue team recommends the Composition API with script setup as the primary approach for new components.' },
      { id: 3, question: 'How do class methods marked with @Emit behave?', options: ['They return the emit function', 'They emit a custom event with the return value as the payload', 'They prevent default behavior', 'They add event listeners'], correctIndex: 1, explanation: 'Methods decorated with @Emit automatically dispatch a custom event with the method return value as the event payload.' }
    ]
  },
  {
    id: 553, slug: 'vue-teleport-patterns', title: 'Advanced Teleport Patterns',
    description: 'Master complex Teleport scenarios including nested teleports, conditional targeting, and multi-app rendering.',
    level: 'advanced', duration: '30 min',
    objectives: ['Implement nested Teleport chains and conditional targeting', 'Build accessible modal and popover systems with Teleport', 'Handle state synchronization between teleported and source components'],
    topics: [
      { id: 'nested-teleport', title: 'Nested Teleport Chains', content: 'Nested Teleports allow content to be teleported through multiple DOM locations. A component can Teleport to a target which itself contains Teleported content from child components. This creates a layering system where different parts of the UI render at different DOM levels. A common pattern is a modal system where the overlay renders at the body level and the modal content renders inside a specific container. You can chain Teleports where the first Teleport renders to body and a child Teleport renders to a portal target inside that body content. The key consideration is that event handling follows the physical DOM tree while the Vue logical tree is preserved. Nested Teleports must coordinate their targets to avoid rendering conflicts. Multiple Teleports to the same target append content in order. Use unique target IDs to prevent content from different features from mixing. The disabled prop on Teleport lets you conditionally disable teleporting for responsive designs.' },
      { id: 'accessible-modals', title: 'Accessible Modal Patterns', content: 'Building accessible modals with Teleport requires attention to focus management and ARIA attributes. Teleport the modal to the body to avoid z-index and overflow issues. When the modal opens move focus to the first focusable element inside the modal. Trap focus within the modal so Tab cycles through modal elements only. When the modal closes restore focus to the element that triggered it. Add role="dialog" and aria-modal="true" to the modal container. Use aria-labelledby to reference the modal title and aria-describedby for the description. Close the modal on Escape key press. Add a visible close button. For screen readers announce the modal opening using an aria-live region. The useFocusTrap composable from VueUse handles focus trapping automatically. Combine Teleport with Transition for smooth modal animations. Manage modal state in a composable or Pinia store so multiple components can trigger modals. Handle scroll locking on the body when the modal is open.' },
      { id: 'multi-app-teleport', title: 'Multi-App Teleport', content: 'Advanced scenarios involve Teleporting between multiple Vue application instances. Micro-frontend architectures may have separate Vue apps that need to share UI elements like notifications or modals. A notification system in one app can Teleport its toast to a shared container rendered by the host application. This requires the target container to exist in the shared DOM. State synchronization between the source app and teleported content uses provide/inject when within the same app or custom event buses between different apps. For cross-app communication use CustomEvents on the window object or a shared store. The teleported component maintains its Vue instance in the source app so reactivity works normally. Lifecycle management must account for the source app unmounting while teleported content is still visible. Use cleanup handlers in onUnmounted to remove teleported elements when the source component is destroyed. Test cross-app Teleport scenarios thoroughly as they introduce complex state management.' }
    ],
    quiz: [
      { id: 1, question: 'What happens when multiple Teleports target the same DOM element?', options: ['Only the first one renders', 'Content is appended in registration order', 'A conflict error is thrown', 'The last one overwrites the others'], correctIndex: 1, explanation: 'Multiple Teleports to the same target append their content in the order they are registered.' },
      { id: 2, question: 'How do you trap focus inside a teleported modal?', options: ['Using CSS only', 'Using useFocusTrap composable from VueUse or manual focus management', 'Focus trapping is automatic', 'It is not possible with Teleport'], correctIndex: 1, explanation: 'useFocusTrap from VueUse or manual event handling cycles focus through focusable elements within the modal.' },
      { id: 3, question: 'Why do you need to manage scroll locking for modals?', options: ['To prevent modals from scrolling', 'To prevent the background page from scrolling when the modal is open', 'To improve modal performance', 'Scroll locking is not necessary'], correctIndex: 1, explanation: 'When a modal is open the background content should not scroll, requiring scroll locking on the body element.' }
    ]
  },
  {
    id: 554, slug: 'vue-error-handling', title: 'Error Handling',
    description: 'Implement comprehensive error handling with error boundaries, global handlers, and recovery patterns.',
    level: 'intermediate', duration: '30 min',
    objectives: ['Set up global error handlers with app.config.errorHandler', 'Use onErrorCaptured for component-level error boundaries', 'Implement error recovery patterns with retries and fallbacks'],
    topics: [
      { id: 'global-error-handler', title: 'Global Error Handler', content: 'Vue provides a global error handler through app.config.errorHandler that catches errors from any component in the application. The handler receives the error the Vue component instance and an info string describing the error source. This is the place to log errors to monitoring services like Sentry or LogRocket. The handler runs for errors in template expressions watchers lifecycle hooks and event handlers. For async errors in promises use window.unhandledrejection event. The handler can return false to prevent the error from being logged to the console. Global handlers are essential for production applications to track runtime errors. Configure the handler during app initialization before mounting. Include error context like component name route and user information in your error reports. The handler does not catch errors in the setup function — use try-catch for that. For development mode consider showing a user-friendly error overlay instead of the default Vue warning.' },
      { id: 'error-boundaries', title: 'Error Boundaries', content: 'Error boundaries catch errors in their child components and display fallback UI instead of crashing the entire application. In Vue 3 the onErrorCaptured lifecycle hook serves as an error boundary. When a child component throws an error during render the parent\'s onErrorCaptured hook fires. The hook receives the error the component instance and an info object. Return false from the hook to prevent the error from propagating further up. A robust error boundary pattern wraps a try-catch around the slot content and displays a fallback template on error. Libraries like vue-error-boundary provide reusable error boundary components. Place error boundaries at strategic points in your component tree — around routes for page-level recovery around complex features for feature-level isolation and around third-party components for library error containment. Each boundary should log the error and provide a retry mechanism or alternative content.' },
      { id: 'error-recovery', title: 'Error Recovery Patterns', content: 'Good error handling includes recovery strategies that help users continue using the application. Retry mechanisms automatically attempt failed operations again with exponential backoff. Wrap API calls in a retry function that catches errors and retries up to a configurable limit. Fallback UI patterns replace failed components with alternative content. A fallback component shows a user-friendly message with a retry button. The ErrorBoundary pattern with a key prop allows resetting by changing the key. State rollback reverts optimistic updates when the server rejects the change. Clear error state after successful operations to prevent stale errors from showing. For form submissions display inline error messages next to the relevant fields. Provide manual retry buttons for user-initiated recovery. Log all errors with context for debugging. For critical features implement circuit breaker patterns that stop retrying after repeated failures and show a maintenance message.' }
    ],
    quiz: [
      { id: 1, question: 'What does app.config.errorHandler catch?', options: ['Only template errors', 'Errors from templates watchers lifecycle hooks and event handlers', 'Only async errors', 'Build-time errors only'], correctIndex: 1, explanation: 'The global error handler catches runtime errors from templates, watchers, lifecycle hooks, and event handlers across all components.' },
      { id: 2, question: 'What lifecycle hook serves as an error boundary in Vue 3?', options: ['onError', 'onErrorCaptured', 'onErrorBoundary', 'onCatch'], correctIndex: 1, explanation: 'onErrorCaptured is the lifecycle hook that catches errors from child components, functioning as an error boundary.' },
      { id: 3, question: 'What is an exponential backoff retry pattern?', options: ['Retrying immediately multiple times', 'Increasing the wait time between each retry attempt', 'Reducing wait time on each retry', 'Retrying only once'], correctIndex: 1, explanation: 'Exponential backoff increases the delay between retry attempts (1s, 2s, 4s, 8s) to avoid overwhelming the server.' }
    ]
  },
  {
    id: 555, slug: 'vue-async-components', title: 'Async Components',
    description: 'Load components on demand with defineAsyncComponent for optimized bundle splitting.',
    level: 'intermediate', duration: '25 min',
    objectives: ['Create async components with defineAsyncComponent', 'Configure loading and error states for async components', 'Use async components with Suspense for seamless loading UX'],
    topics: [
      { id: 'async-component-basics', title: 'Async Component Basics', content: 'Async components are Vue components whose definitions are loaded lazily using dynamic imports. defineAsyncComponent accepts a factory function that returns a Promise resolving to the component module. This triggers code splitting — the component code is bundled separately and loaded only when the component is first rendered. Basic usage is defineAsyncComponent(() => import("./MyComponent.vue")). The component shows nothing or a loading indicator while the definition loads then renders normally. Async components reduce initial bundle size by deferring code that may not be needed immediately. They work with both the Options API and Composition API. The component name is inferred from the import or can be specified for debugging. Async components are Suspense-compatible and work with the Suspense component for coordinated loading states. Dynamic imports support conditional loading based on feature flags or user permissions.' },
      { id: 'async-options', title: 'Async Component Options', content: 'defineAsyncComponent accepts an options object for advanced configuration. The loadingComponent option specifies a component to display while the async component loads. The delay option controls how long to wait before showing the loading component (default 200ms). This prevents flash of loading states for fast loads. The errorComponent displays when the component fails to load. The timeout option triggers the error component after a specified duration in milliseconds. The onError option provides a hook for handling loading failures with retry logic. A loading component should be simple and fast to render since it appears briefly. Error components should explain the failure and provide a retry mechanism. The suspensible option controls whether the component integrates with Suspense. When true the component loading is managed by the parent Suspense boundary. These options create a polished loading experience without manual state management.' },
      { id: 'async-patterns', title: 'Async Component Patterns', content: 'Several patterns leverage async components effectively. Route-level code splitting uses async components in router definitions so each page loads independently. Feature-based splitting loads entire features like admin panels or settings pages only when accessed. Conditional async loading uses feature flags to load different component implementations. For example load a premium component only for paid users. The fallback pattern renders a simple placeholder during load and shows the rich component once loaded. Error recovery patterns provide retry buttons in error components that re-trigger the async import. Prefetching loads async components before they are needed using link preload headers or hover-based triggers. The virtual scrolling pattern combines async components with virtual scroll to load component definitions for visible items. Combining async components with Suspense provides coordinated loading where the Suspense fallback covers all pending async components. Monitor async component loading in production to identify slow-loading chunks.' }
    ],
    quiz: [
      { id: 1, question: 'What does defineAsyncComponent do?', options: ['Creates a synchronous component', 'Returns a component that loads its definition lazily via dynamic import', 'Compiles components at build time', 'Registers components globally'], correctIndex: 1, explanation: 'defineAsyncComponent wraps a dynamic import to lazily load the component definition only when it is first rendered.' },
      { id: 2, question: 'What is the purpose of the delay option in async components?', options: ['Delays the component mount', 'Prevents flash of loading states for fast loads by waiting before showing the loading component', 'Delays the dynamic import', 'Adds animation delay'], correctIndex: 1, explanation: 'The delay option (default 200ms) waits before showing the loading component so fast loads do not flash a loading indicator.' },
      { id: 3, question: 'How do async components work with Suspense?', options: ['They do not work together', 'Suspense detects the pending async component and shows its fallback until loaded', 'Suspense blocks the async component', 'You must configure special options'], correctIndex: 1, explanation: 'Suspense automatically detects pending async components in its subtree and displays the fallback slot until all async components resolve.' }
    ]
  },
  {
    id: 556, slug: 'vue-lazy-loading', title: 'Lazy Loading Images and Components',
    description: 'Implement lazy loading for images components and routes to improve initial page load performance.',
    level: 'intermediate', duration: '30 min',
    objectives: ['Implement image lazy loading with native loading attribute and Intersection Observer', 'Create a reusable lazy loading directive for Vue', 'Combine lazy loading with virtual scrolling for large media galleries'],
    topics: [
      { id: 'image-lazy-loading', title: 'Image Lazy Loading', content: 'Image lazy loading defers loading off-screen images until they enter the viewport. The simplest approach uses the native HTML loading="lazy" attribute on img tags. This is supported in all modern browsers and requires zero JavaScript. The browser automatically defers loading images below the fold. For more control use the Intersection Observer API which lets you trigger loading based on custom thresholds. Create a Vue directive that observes when the image enters the viewport and swaps the data-src attribute to src. This technique works with placeholder images that display immediately while the full image loads in the background. The v-lazy-load directive pattern accepts a placeholder image URL and transitions to the full image when visible. For responsive images use srcset with lazy loading to serve appropriately sized images. Combine lazy loading with blur-up placeholders using a tiny base64 thumbnail that blurs while the full image loads.' },
      { id: 'component-lazy-loading', title: 'Component Lazy Loading', content: 'Component lazy loading loads component definitions only when they are needed rather than upfront. Vue provides defineAsyncComponent for component-level code splitting. Use it for components that are below the fold or behind user interactions like modals drawers and tabs. The component is bundled separately and loaded via dynamic import when first rendered. Combine with Suspense for a polished loading experience. For tab panels lazy load each panel\'s content only when the tab is selected. For forms lazy load complex field components like date pickers and rich text editors. For admin interfaces lazy load entire feature modules. The key is identifying which components are not needed for initial render and converting them to async components. Monitor your bundle analyzer to find large components that benefit from lazy loading. Be careful not to create too many small chunks which can cause excessive HTTP requests.' },
      { id: 'lazy-loading-patterns', title: 'Lazy Loading Patterns', content: 'Several patterns combine lazy loading for optimal performance. Prefetch on hover loads component chunks when the user hovers over a navigation link giving instant transitions. Route-based prefetching loads chunks for likely next routes based on navigation patterns. Progressive loading shows a simple version first then progressively loads enhanced components. The skeleton pattern displays placeholder shapes that match the layout while content loads. Infinite scroll combines lazy loading with scrolling to load content and components as the user scrolls. The fold-based pattern uses Intersection Observer to determine what is above the fold and lazy loads everything else. Feature flag based loading loads different component implementations based on feature flags. Geographic loading loads region-specific components based on user location. A/B testing loads variant components only for users in the test group. Always measure the impact of lazy loading on Core Web Vitals including Largest Contentful Paint and Cumulative Layout Shift.' }
    ],
    quiz: [
      { id: 1, question: 'What is the simplest way to lazy load images in modern browsers?', options: ['Using JavaScript only', 'Adding loading="lazy" attribute to img tags', 'Using Intersection Observer only', 'Converting images to base64'], correctIndex: 1, explanation: 'The native loading="lazy" attribute is supported in modern browsers and defers image loading without any JavaScript.' },
      { id: 2, question: 'When should you lazy load component definitions?', options: ['For every component', 'For components not needed for initial render like modals and tabs', 'Only for large components', 'Never for production'], correctIndex: 1, explanation: 'Lazy load components that are not visible on initial render such as modals, tab panels, and admin features.' },
      { id: 3, question: 'What is the prefetch-on-hover pattern?', options: ['Loading images on hover', 'Loading route chunks when the user hovers over a navigation link for instant transitions', 'Prefetching API data', 'Loading styles on hover'], correctIndex: 1, explanation: 'Prefetch-on-hover loads the JavaScript chunk for a route when the user hovers over its link, making navigation feel instant.' }
    ]
  },
  {
    id: 557, slug: 'vue-head-management', title: 'Head Manager',
    description: 'Manage document head elements dynamically for SEO and social media optimization.',
    level: 'intermediate', duration: '25 min',
    objectives: ['Use @vueuse/head or Unhead for dynamic head management', 'Set per-page meta tags titles and Open Graph data', 'Integrate head management with Vue Router for SPA navigation'],
    topics: [
      { id: 'head-management-overview', title: 'Head Management Overview', content: 'Head management in Vue SPAs requires dynamically updating the document title meta tags and link elements as users navigate. Unlike traditional server-rendered pages where each URL has its own HTML response SPAs serve a single HTML shell. Head management libraries inject and update head elements at runtime. Unhead is the modern successor to @vueuse/head and provides a composable API for managing head elements. useHead sets title meta tags link elements and script tags reactively. The useSeoMeta composable provides a simplified API for common SEO meta tags. Nuxt 3 includes Unhead integration with useHead and useSeoMeta built in. Head elements update automatically when the route changes if you define them per-page. For social media sharing ensure Open Graph and Twitter Card meta tags are set correctly for each page.' },
      { id: 'head-api', title: 'Head API Usage', content: 'The head management API provides several composable functions. useHead takes a head object with properties for title template meta and link. Title can be a string or a function that receives the page context. Template strings support %s as a placeholder for the page title. Meta tags are defined as an array of objects with name property content and optional key for deduplication. Link tags define canonical URLs stylesheets and preconnect hints. useSeoMeta provides a friendlier API for SEO-specific tags including title description og:title og:image and twitter:card. All values can be reactive refs that update when state changes. The head manager handles deduplication so defining the same meta tag multiple times replaces rather than duplicates it. Server-side rendering with head management serializes head elements into the HTML response. Client-side hydration picks up the serialized head state.' },
      { id: 'router-integration', title: 'Router Integration', content: 'Integrating head management with Vue Router ensures head elements update on navigation. The simplest approach defines head metadata in each page component using useHead or useSeoMeta. When the component mounts the head updates and when it unmounts the previous head state is restored. For global head defaults like site title and description set them in the app component. Per-page head overrides the global defaults. The router afterEach hook can update head elements based on route meta. Define title and meta in route meta objects and apply them in a global after-each guard. For nested routes the deepest route\'s head takes precedence. Dynamic head values based on route params require fetching data before setting the head. Use onBeforeRouteEnter or the route before-enter guard to load data then set the head. For error pages define appropriate head metadata for 404 and 500 pages.' }
    ],
    quiz: [
      { id: 1, question: 'What is the modern library for head management in Vue?', options: ['vue-meta', 'Unhead (@vueuse/head)', 'vue-head', 'document.head-manager'], correctIndex: 1, explanation: 'Unhead (successor to @vueuse/head) is the modern head management library with composable APIs for managing document head elements.' },
      { id: 2, question: 'How do head elements update on route changes?', options: ['They do not update automatically', 'By defining useHead in each page component which updates on mount and restores on unmount', 'By manually editing the DOM', 'By refreshing the page'], correctIndex: 1, explanation: 'Defining useHead in page components automatically updates head elements when the component mounts and restores them on unmount during navigation.' },
      { id: 3, question: 'What does useSeoMeta provide over useHead?', options: ['Better performance', 'A simplified API specifically for SEO meta tags', 'More head elements', 'Server-side rendering support'], correctIndex: 1, explanation: 'useSeoMeta provides a simplified and type-safe API for common SEO meta tags like title description and Open Graph properties.' }
    ]
  },
  {
    id: 558, slug: 'vue-meta-tags', title: 'Meta Tags and Open Graph',
    description: 'Configure Open Graph Twitter Card and structured data meta tags for social media and SEO.',
    level: 'intermediate', duration: '30 min',
    objectives: ['Set Open Graph meta tags for social media sharing previews', 'Configure Twitter Card meta tags for Twitter sharing', 'Implement JSON-LD structured data for search engines'],
    topics: [
      { id: 'open-graph', title: 'Open Graph Protocol', content: 'Open Graph meta tags control how your pages appear when shared on social media platforms like Facebook LinkedIn and Discord. The required tags are og:title (page title), og:type (website article product), og:image (preview image URL), and og:url (canonical URL). Additional tags include og:description for the preview text og:site_name for your site name and og:locale for language. Images should be 1200x630 pixels for optimal display across platforms. Use absolute URLs for og:image since social media crawlers cannot resolve relative URLs. Set og:type to website for home pages article for blog posts and product for product pages. Each page should have unique Open Graph tags reflecting its content. Dynamic pages generate OG tags based on page data like product name and image. Test your Open Graph tags using Facebook Sharing Debugger and LinkedIn Post Inspector to verify correct rendering.' },
      { id: 'twitter-cards', title: 'Twitter Cards', content: 'Twitter Card meta tags control how your pages appear when shared on Twitter/X. The twitter:card meta tag specifies the card type — summary for small image summary_large_image for large image player for video and app for app downloads. twitter:title sets the card title. twitter:description sets the card text. twitter:image sets the preview image. twitter:site is your Twitter username starting with @. twitter:creator is the content author\'s Twitter username. For summary_large_image use an image at least 300x157 pixels. For summary use at least 144x144 pixels. Twitter validates cards using the Card Validator tool. Dynamic Twitter cards generate based on page content like article titles and featured images. Combine Twitter Card tags with Open Graph tags since Twitter falls back to OG tags when Twitter-specific tags are missing. Ensure consistent image and text across both tag sets for a professional appearance.' },
      { id: 'json-ld', title: 'JSON-LD Structured Data', content: 'JSON-LD structured data helps search engines understand your page content and display rich results. It uses a script tag with type application/ld+json containing a JSON object describing the page. Common schemas include Article for blog posts Product for e-commerce LocalBusiness for businesses and FAQPage for FAQ sections. Google supports structured data for rich snippets like star ratings price ranges and event dates. Define the @context as https://schema.org and @type as the schema type. Include required properties for each schema type — Article needs headline datePublished and author. Product needs name image offers and description. Dynamic JSON-LD generates schema objects based on page data. Use the useHead composable to inject JSON-LD script tags. Validate structured data using Google Rich Results Test. Structured data improves click-through rates by displaying enhanced search results with rich information directly in search pages.' }
    ],
    quiz: [
      { id: 1, question: 'What are the required Open Graph meta tags?', options: ['og:title and og:description', 'og:title og:type og:image and og:url', 'og:image and og:url only', 'All OG tags are optional'], correctIndex: 1, explanation: 'The Open Graph protocol requires four properties: og:title, og:type, og:image, and og:url for proper social media previews.' },
      { id: 2, question: 'What image size is recommended for Open Graph preview images?', options: ['300x300', '1200x630', '600x300', 'Any size works'], correctIndex: 1, explanation: '1200x630 pixels is the recommended Open Graph image size for optimal display across all social media platforms.' },
      { id: 3, question: 'What does JSON-LD structured data help with?', options: ['CSS styling', 'Search engine understanding and rich result display', 'JavaScript execution', 'Image optimization'], correctIndex: 1, explanation: 'JSON-LD structured data helps search engines understand page content and display enhanced rich results in search pages.' }
    ]
  },
  {
    id: 559, slug: 'vue-ssr-streaming', title: 'Streaming SSR',
    description: 'Improve SSR performance with HTML streaming using renderToNodeStream and Suspense.',
    level: 'advanced', duration: '35 min',
    objectives: ['Understand how HTML streaming improves SSR time to first byte', 'Configure Nuxt 3 and Nitro for streaming SSR responses', 'Use Suspense with streaming to progressively send rendered HTML to the browser'],
    topics: [
      { id: 'streaming-concepts', title: 'Streaming SSR Concepts', content: 'Streaming SSR sends the HTML response to the browser in chunks as the server renders each part of the page. Traditional SSR waits for the entire page to render before sending any HTML. Streaming starts sending the document head and initial markup immediately while the server continues rendering the rest of the page. This reduces time to first byte and perceived load time. The browser begins parsing and displaying the initial HTML while receiving subsequent chunks. Streaming works by converting the Vue virtual DOM to a Node.js stream instead of a string. The renderToNodeStream function returns a readable stream that sends HTML chunks. Suspense integrates naturally with streaming — async components that resolve while streaming add their HTML to the next chunk. The browser progressively hydrates rendered HTML chunks as they arrive. This approach is especially beneficial for pages with slow API calls where the shell renders immediately while data loads.' },
      { id: 'streaming-setup', title: 'Streaming SSR Setup', content: 'Setting up streaming SSR in Nuxt 3 requires Nitro as the server engine. Nuxt 3 supports streaming by default when using SSR mode. Nitro uses the Node.js HTTP module which supports streaming responses. For custom server setups use renderToNodeStream from vue/server-renderer. The stream is piped to the HTTP response object. Set the Content-Type header to text/html and the Transfer-Encoding header to chunked. Suspense boundaries within the streamed HTML resolve independently — each async component sends its rendered HTML when it resolves. Error handling in streaming requires catching errors in the stream and sending error HTML. The pipeToWebStreams function in newer Vue versions converts the render result to a Web ReadableStream compatible with modern server runtimes like Deno and Cloudflare Workers. Test streaming SSR with slow network simulation to verify the progressive loading experience.' },
      { id: 'streaming-patterns', title: 'Streaming SSR Patterns', content: 'Several patterns leverage streaming SSR effectively. The shell pattern sends the page skeleton immediately including navigation and layout while the main content loads asynchronously. The progressive enhancement pattern streams the basic HTML first then adds interactive JavaScript and enhanced components. The priority streaming pattern renders critical content first like SEO meta tags and above-the-fold content while deferring below-the-fold content. Suspense with streaming automatically handles the ordering — resolved components stream their HTML immediately while pending components stream their fallback then replace it. Error boundaries in streaming catch rendering errors and send error HTML for the failed section without breaking the entire stream. The selective hydration pattern hydrates critical components first for faster interactivity. Measure streaming performance using Time to First Byte (TTFB) and First Contentful Paint (FCP) metrics. Streaming SSR is supported by Nitro, Vercel Edge Functions, and Cloudflare Workers for edge deployment.' }
    ],
    quiz: [
      { id: 1, question: 'How does streaming SSR improve performance?', options: ['It renders fewer components', 'It sends HTML in chunks starting with the shell while rendering continues', 'It skips server rendering', 'It caches the entire page'], correctIndex: 1, explanation: 'Streaming SSR sends the initial HTML shell immediately while the server continues rendering, reducing time to first byte and perceived load time.' },
      { id: 2, question: 'How does Suspense integrate with streaming SSR?', options: ['It blocks the stream', 'Each async component sends its HTML as it resolves within the stream', 'Suspense is not used with streaming', 'It sends all HTML at once'], correctIndex: 1, explanation: 'Suspense resolves async components independently during streaming, sending their HTML chunks as they become available.' },
      { id: 3, question: 'What metric does streaming SSR primarily improve?', options: ['Bundle size', 'Time to First Byte (TTFB)', 'Memory usage', 'Build time'], correctIndex: 1, explanation: 'Streaming SSR reduces TTFB by sending the initial HTML response immediately instead of waiting for the entire page to render.' }
    ]
  },
  {
    id: 560, slug: 'vue-architecture', title: 'Vue Application Architecture',
    description: 'Design scalable Vue applications with feature-based architecture and proven organizational patterns.',
    level: 'advanced', duration: '45 min',
    objectives: ['Structure Vue projects with feature-based folder organization', 'Implement clean architecture patterns with composables stores and services', 'Design scalable component APIs and state management boundaries'],
    topics: [
      { id: 'project-structure', title: 'Project Structure', content: 'A well-organized Vue project follows a feature-based directory structure. The top level separates shared code from feature code. The src/features directory contains feature modules like auth dashboard and settings. Each feature has its own components composables stores and types. The src/shared directory contains components composables and utilities used across multiple features. The src/core directory holds infrastructure code like HTTP clients authentication and logging. This structure scales as the team and codebase grow. Feature modules are self-contained — they export a public API and keep internal implementation private. Components within a feature are organized by type or by component hierarchy. Composables live alongside the components that use them or in a composables subdirectory. Types specific to a feature live in the feature directory while shared types live in the shared directory. This organization supports code splitting at the feature level.' },
      { id: 'architecture-patterns', title: 'Architecture Patterns', content: 'Vue applications benefit from several architectural patterns. The container/presentational pattern separates data-fetching container components from pure display presentational components. Containers handle logic and pass data down as props. Presentational components are reusable and focused on rendering. The composable pattern encapsulates stateful logic into reusable functions organized by domain. The service pattern abstracts API calls and external integrations behind a clean interface. Services are imported by composables which are used by components. The store pattern uses Pinia for shared state that spans multiple features. Each feature may have its own store with a clear API boundary. The event bus pattern (used sparingly) handles cross-feature communication. The dependency injection pattern uses provide/inject to share services through the component tree. Choose patterns based on your team size and application complexity. Start simple and add patterns as needed rather than over-engineering upfront.' },
      { id: 'scaling-strategies', title: 'Scaling Strategies', content: 'Scaling a Vue application requires attention to code organization performance and team coordination. Code splitting at the feature level ensures each feature loads independently. Lazy load features that users access infrequently. Pinia modules separate global state by domain to avoid monolithic stores. Component libraries shared across features ensure visual consistency. Design tokens define shared design values like colors and spacing. Monorepo tools like Nx or Turborepo manage multiple packages in a single repository. Micro-frontend architectures split the application into independently deployable pieces using Module Federation. For team coordination establish coding standards through ESLint and Prettier configs. Create shared packages for common utilities components and types. Use feature flags to safely deploy incomplete features. Performance budgets prevent bundle size regressions. Automated testing at the component integration and E2E levels catches regressions. Monitor production performance with real user monitoring tools.' }
    ],
    quiz: [
      { id: 1, question: 'Why use feature-based directory structure?', options: ['It reduces file count', 'It co-locates related code making features easier to navigate and maintain', 'It improves rendering speed', 'It is required by Vue'], correctIndex: 1, explanation: 'Feature-based organization groups related components, composables, stores, and types by domain feature for easier navigation and maintenance.' },
      { id: 2, question: 'What is the container/presentational pattern?', options: ['Using CSS containers', 'Separating data-fetching logic into containers from pure display into presentational components', 'A layout pattern', 'A state management pattern'], correctIndex: 1, explanation: 'Container components handle logic and data fetching while presentational components focus purely on rendering UI from received props.' },
      { id: 3, question: 'When should you introduce architectural patterns in a Vue project?', options: ['From day one for every project', 'When the team and codebase grow and patterns are needed', 'Never', 'Only after the project fails'], correctIndex: 1, explanation: 'Start simple and introduce patterns as the codebase and team grow, adding complexity only when it solves real problems.' }
    ]
  }
];
